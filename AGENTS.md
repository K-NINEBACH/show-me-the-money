# 급여 패스북 — 작업 지침

개인 가계부 PWA "내 돈 챙겨줘". 급여 후 과소비를 막는 게 목적이고, **혼자 쓰는
도구**로 확정됐다. 자세한 내력은 `PROJECT.md`에 있다 — 작업 전에 읽을 것.

## 말투

- **호칭을 쓰지 않는다.** '형'·'형님' 금지. 호칭 없이 존댓말로 쓴다.
- **전부 한글로.** 문서의 절 제목과 표 머리글까지. 번역 창을 여는 게 번거롭다.
- 실행할 SQL이나 명령은 파일 링크 대신 **대화창에 복붙 가능한 코드블록**으로.

## 스택과 실행

React 18 + Vite 5. 런타임 의존성은 `lucide-react`뿐이고 **스타일 라이브러리 없이
인라인 style 객체만** 쓴다. 차트 라이브러리도 없다.

```
npm install
npm run dev      # 개발
npm run build    # 배포 빌드 → dist/
```

```
src/
├── App.jsx              셸 — 게이트, 탭 라우팅, 그리고 ctx(공용 계산)
├── lib/
│   ├── constants.js     STORAGE_KEY, PALETTE, QUICK_AMOUNTS
│   ├── theme.js         THEMES, useTheme, paperCard/inputSty/primaryBtn
│   ├── data.js          defaultData, migrate, 날짜·금액 유틸, netAmount, fixedInfo
│   ├── native.js        안드로이드 껍데기와의 다리 (없으면 조용히 넘어감)
│   └── auto-record.js   결제 알림을 확인 없이 기록으로 넣는 규칙
├── components/          common.jsx, ErrorBoundary, payment-inbox
└── screens/             Gates · Home · Add · Ledger · Calendar · Settings
```

화면을 고칠 땐 `screens/` 안의 해당 파일만 보면 된다. 여러 화면이 같이 걸리는
계산(카드값·예산 등)은 `App.jsx`의 `ctx` 구성부에 있다.

## 절대 어기면 안 되는 것

**데이터는 서버가 아니라 브라우저 localStorage에만 있다.**

- 데이터 구조를 바꾸면 `lib/data.js`의 `migrate()`를 **반드시 함께** 고친다.
  안 그러면 기존 사용자의 기록이 조용히 사라진다.
- **Vercel은 같은 프로젝트·같은 주소로만 재배포한다.** 주소가 바뀌면
  localStorage에 접근할 수 없게 되어 기록이 통째로 날아간다.
  현재 주소: `https://show-me-the-money-k-ninebach0.vercel.app`

## 되풀이된 버그 — 라벨과 계산이 어긋난다

이 저장소에서 **다섯 번** 나왔다. 새 요약·비교 숫자를 만들 때 라벨이 말하는 것과
실제 계산 대상을 반드시 대조할 것.

| 언제 | 무엇이 어긋났나 |
| --- | --- |
| 2026-08-24 | "최신순"이 사실은 등록순(날짜가 아니라 id 시각으로 정렬) |
| 2026-08-24 | "총 지출 N건"의 건수가 그 금액에 들어간 건수와 달랐다 |
| 2026-08-24 | "지난달 대비"가 기간 길이가 다른 비교였다 |
| 2026-09-08 | 정산받은 금액(`reimbursedAmount`)을 어느 합계도 안 읽고 있었다 |
| 2026-09-09 | 자동 등록이 카드 할부와 짝을 지어 그 달 카드값이 두 배가 됐다 |

정산받은 금액 건(2026-09-08) 때문에 `netAmount(e)`가 생겼다 — **지출 합계는 전부
이걸로 낸다.**
줄마다 보이는 금액은 실제 결제액이라 단순히 더하면 합계와 다른데, 그 차이가
곧 돌려받은 돈이다. 그래서 총계 옆에 "정산받은 N원은 뺐어요"를 적어 둔다.

## 여유(게이지)에 무엇이 들어가나

```
여유 = 목표 − ( 현금지출 + 고정지출 + 미결제 카드값 + 이번달 할부몫 − 카드 정산분 )
```

- **통장 입출금(`balanceEntries`)은 여유에 안 들어간다.** 잔고를 맞추는 용도다.
  홈의 입금·출금·이체 버튼도 마찬가지다.
- 현금 지출은 지출 기록 쪽에서 빠진다. 딸려 생기는 출금은 잔액만 줄인다
  (이중 차감 없음).
- 미해결: 카드값 `결제하기`를 누르면 `bill`이 0이 되면서 여유가 그만큼 늘어난다.
  돈은 방금 나갔는데 안 쓴 것이 된다. 고치려면 카드를 '이번 달 카드지출'로
  세야 하는데, 그러면 지난달 미결제분이 이번 달 여유에서 빠진다. 사용자와
  합의가 필요한 자리다.

## 자동 등록 (2026-09-08)

안드로이드 껍데기가 카드·은행 알림을 읽어 넘기면 `lib/auto-record.js`가 처리한다.

- **카드 승인** → 지출 기록 (여유에 반영)
- **은행 입출금** → 통장 입출금 기록 (잔고만)
- 애매하면 넣지 않고 기록 탭의 알림함에 남긴다.
  **놓친 건 나중에 넣으면 되지만 잘못 들어간 건 찾기 어렵다.**
- **알림함은 휴대폰에 저장된다**(`INBOX_KEY`, 가계부 데이터와 다른 칸). 껍데기는 웹이
  가져가는 순간 큐를 비우므로, 예전엔 보류된 알림이 앱을 닫으면 어디에도 안 남았다.
- **한 번 보류한 것은 다시 자동으로 판단하지 않는다.** 그러지 않으면 미뤄 둔 것이
  나중에 엉뚱한 데이터 변경에 편승해 들어간다(백업 복원 때 실제로 그랬다).
- 넣은 것에는 `auto: true` — 내역에서 '자동' 배지로 보인다.
- **사용자는 "확인 안 누르고 전부 자동으로"를 원한다**(2026-09-11). 알림함에 남는 건
  어느 카드·통장인지 정말 모를 때와 금액을 못 읽을 때뿐이어야 한다. 새 규칙을 더할 때
  '애매하면 보류'보다 '제대로 판단해서 처리'를 먼저 찾을 것.
- **같은 거래인지는 결제 시각으로 가른다.** 자동 기록엔 문자의 시각을 `autoTime`으로
  남긴다. 같은 날·같은 금액이어도 시각이 다르면 다른 거래(지하철 두 번), 같으면 같은
  거래(앱 알림과 문자로 두 번 온 것). 손으로 적은 것·앱이 만든 출금은 시각이 없어
  같은 날·같은 금액이면 같은 거래로 보고 **넘긴다**(보류 아님, 알림 문구로 알림).
- 이미 받아 처리한 알림 문구는 `SEEN_KEY`에 남겨 다시 안 받는다(재시작 시 껍데기가
  알림창을 다시 훑어 넘기므로).
- **결제했다가 취소하면**: 둘 다 기록 전이면(알림함 포함) 서로 지운다. 이미 들어갔으면
  그 기록을 빼고 카드값을 되돌린다 — 자동 기록을 먼저, 없으면 손으로 적은 것을.
  이번 달·정산 안 붙인 것만. 자동 등록한 할부의 취소면 그 할부 항목을 지운다.
- **할부 결제는 '할부(고정지출)'로 자동 등록한다**(월 금액 = 총액 ÷ 개월, 나머지는
  첫 달). 지출로 넣으면 총액이 이번 달 카드값에 통째로 잡힌다. 개월 수를 못 읽으면 보류.
- 카드 문자 끝의 '누적○○원'은 결제를 막지 않는다(현대카드는 늘 붙는다). 대신 '결제
  예정·청구·안내'는 결제로 안 본다.
- **어느 은행·카드인지는 문구가 아니라 보낸 앱(패키지)으로 가린다.** 은행 이름이
  문구에 아예 없는 형식이 흔하다(KB스타뱅킹). 보낸 곳을 아는데 그 이름의 통장이
  없으면 글자로 찍지 않는다 — 문구에 든 남의 회사 이름에 붙어 버린다.
- 알림은 앱을 다시 열 때와 **켜 둔 동안 15초마다** 가져온다. 예전엔 '돌아옴'
  신호에만 기대서, 앱을 보는 중에 온 알림은 나갔다 와야 들어왔다.
- 고정지출과 짝이 맞으면 그 항목을 **처리 완료로 표시**한다(`paidMonths`).
  버튼이 사라지므로 며칠 뒤에 눌러도 중복이 안 생긴다.
  적는 달은 '지금'이 아니라 **그 결제가 일어난 달**이다 — 12/31 알림을 1/1에
  읽으면 양쪽 달이 다 틀어진다.
- 금액이 같은 고정지출이 여럿이면 문구의 적요로 가르고, 못 가르면 보류한다.
- **짝은 카드매달반복만.** 카드 할부는 이미 `fixedPortion`으로 카드값에 들어가
  있어서, 짝을 지어 `bill`에 또 더하면 그 달 카드값이 두 배가 된다. 할부 회차와
  금액이 같은 승인은 아예 자동으로 안 넣는다(알림함으로).

## 안드로이드 껍데기

`D:\passbook-android` (별도 폴더, 이 저장소 밖).

웹앱은 알림을 읽을 수 없어서(권한이 아니라 기능이 없다) 그것만 네이티브로 뺐다.
화면은 하나도 새로 만들지 않는다 — WebView로 이 앱을 띄운다.

```
D:\dev\jdk17          JDK
D:\dev\android-sdk    SDK
D:\dev\gradle-8.7     Gradle

빌드:  JAVA_HOME=D:\dev\jdk17  gradle assembleRelease --no-daemon
결과:  app\build\outputs\apk\release\app-release.apk
```

**서명 키를 잃으면 업데이트를 덮어 설치할 수 없다.** 지웠다 깔아야 하고 그러면
앱 안의 기록이 날아간다. 키: `E:\클로드백업\passbook.jks` (비밀번호 `passbook`).

웹만 고쳤으면 **APK를 다시 깔 필요가 없다** — 원격 주소를 띄우므로 푸시하면
앱을 다시 켤 때 반영된다. 네이티브(알림 규칙·백업·앱 이름)를 고칠 때만 재설치.

자동 백업은 껍데기가 한다 — 화면을 벗어날 때 `Download/내돈챙겨줘/`에 하루
한 파일, 30일치. 앱 전용 폴더가 아닌 이유는 앱을 지워도 남아야 하기 때문이다.
되돌리는 건 설정 → 데이터 백업 → '휴대폰에 저장된 백업'에서 한다.
