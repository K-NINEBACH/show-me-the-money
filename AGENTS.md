# 급여 패스북 — 작업 지침

개인 가계부 PWA "내 돈 챙겨줘". 급여 후 과소비를 막는 게 목적이고, **혼자 쓰는
도구**로 확정됐다. 자세한 내력은 `PROJECT.md`에 있다 — 작업 전에 읽을 것.

## 말투

- **호칭을 쓰지 않는다.** '형'·'형님' 금지. 호칭 없이 존댓말로 쓴다.
- **전부 한글로.** 문서의 절 제목과 표 머리글까지. 번역 창을 여는 게 번거롭다.
- 실행할 SQL이나 명령은 파일 링크 대신 **대화창에 복붙 가능한 코드블록**으로.

## 스택과 실행

React 18 + Vite 5. 런타임 의존성은 `lucide-react`뿐이고 **스타일 라이브러리 없이
인라인 style 객체만** 쓴다. 차트 라이브러리도 없다.

```
npm install
npm run dev      # 개발
npm run build    # 배포 빌드 → dist/
```

```
src/
├── App.jsx              셸 — 게이트, 탭 라우팅, 그리고 ctx(공용 계산)
├── lib/
│   ├── constants.js     STORAGE_KEY, PALETTE, QUICK_AMOUNTS
│   ├── theme.js         THEMES, useTheme, paperCard/inputSty/primaryBtn
│   ├── data.js          defaultData, migrate, 날짜·금액 유틸, netAmount, fixedInfo
│   ├── native.js        안드로이드 껍데기와의 다리 (없으면 조용히 넘어감)
│   └── auto-record.js   결제 알림을 확인 없이 기록으로 넣는 규칙
├── components/          common.jsx, ErrorBoundary, payment-inbox
└── screens/             Gates · Home · Add · Ledger · Calendar · Settings
```

화면을 고칠 땐 `screens/` 안의 해당 파일만 보면 된다. 여러 화면이 같이 걸리는
계산(카드값·예산 등)은 `App.jsx`의 `ctx` 구성부에 있다.

## 절대 어기면 안 되는 것

**데이터는 서버가 아니라 브라우저 localStorage에만 있다.**

- 데이터 구조를 바꾸면 `lib/data.js`의 `migrate()`를 **반드시 함께** 고친다.
  안 그러면 기존 사용자의 기록이 조용히 사라진다.
- **Vercel은 같은 프로젝트·같은 주소로만 재배포한다.** 주소가 바뀌면
  localStorage에 접근할 수 없게 되어 기록이 통째로 날아간다.
  현재 주소: `https://show-me-the-money-k-ninebach0.vercel.app`

## 되풀이된 버그 — 라벨과 계산이 어긋난다

이 저장소에서 **여섯 번** 나왔다. 새 요약·비교 숫자를 만들 때 라벨이 말하는 것과
실제 계산 대상을 반드시 대조할 것.

| 언제 | 무엇이 어긋났나 |
| --- | --- |
| 2026-08-24 | "최신순"이 사실은 등록순(날짜가 아니라 id 시각으로 정렬) |
| 2026-08-24 | "총 지출 N건"의 건수가 그 금액에 들어간 건수와 달랐다 |
| 2026-08-24 | "지난달 대비"가 기간 길이가 다른 비교였다 |
| 2026-09-08 | 정산받은 금액(`reimbursedAmount`)을 어느 합계도 안 읽고 있었다 |
| 2026-09-09 | 자동 등록이 카드 할부와 짝을 지어 그 달 카드값이 두 배가 됐다 |
| 2026-09-13 | 설정의 "아직 앱으로 안 가져옴"이 사실은 '웹 처리 기록에서 못 찾음'이었다(대기 0건인데 떴다). 통장 출금도 "결제 N건"이라 불렀다 |
| 2026-09-16 | 받은편지함이 **못 읽고 넘긴 것도 '적용함'으로** 적었다. 옛 판이 먼저 받아 넘기면 새 판이 와도 '이미 넣음'으로 건너뛰어 영영 안 들어간다(롯데 할부 85만 원이 두 번 잡혔다) |

정산받은 금액 건(2026-09-08) 때문에 `netAmount(e)`가 생겼다 — **지출 합계는 전부
이걸로 낸다.**
줄마다 보이는 금액은 실제 결제액이라 단순히 더하면 합계와 다른데, 그 차이가
곧 돌려받은 돈이다. 그래서 총계 옆에 "정산받은 N원은 뺐어요"를 적어 둔다.

## 여유(게이지)에 무엇이 들어가나

```
여유 = 목표 − ( 현금지출 + 고정지출 + 미결제 카드값 + 이번달 할부몫 − 카드 정산분 )
```

- **통장 입출금(`balanceEntries`)은 여유에 안 들어간다.** 잔고를 맞추는 용도다.
  홈의 입금·출금·이체 버튼도 마찬가지다.
- 현금 지출은 지출 기록 쪽에서 빠진다. 딸려 생기는 출금은 잔액만 줄인다
  (이중 차감 없음).
- 카드값 `결제하기`를 누르면 `bill`이 0이 되면서 여유가 그만큼 늘어난다. **이게 맞다**
  (2026-09-09 사용자와 합의 — 현금 흐름식). 갚아야 할 돈이 사라졌으니 묶어 둔 예산이
  풀리는 것이다. '이번 달 카드지출'로 바꾸면 지난달 카드값이 이번 달 여유를 깎는다.

- **카드값은 누적 한 칸(`bill`)이라 '언제 낸 돈인지'를 따로 적는다**(2026-09-13).
  `installPaid[달]` — 결제하기로 낸 이번 달 할부 몫(안 적으면 내자마자 다시 떠서 두 번
  냈다). `paidAtMs` — 마지막으로 낸 시각. 그 전에 만든 카드 지출은 지우거나 고쳐도 지금
  카드값을 안 건드린다(예전엔 지난달 기록을 정리하면 이번 달 카드값이 줄었다).

## 홈은 한눈에 — 누르지 않고 다 보이게 (2026-09-14)

**이 앱의 핵심**(사용자): "굳이 추가적인 제스처나 행동, 입력 없이 자동으로 나의 자산(통장,
카드값, 잔여 여유분 등등)을 한눈에 쉽게 볼 수 있는 게 핵심." 그 전에 "지저분하다, 컴팩트하게"도.
→ **보는 건 누르지 않아도 되고, 누르는 건 가끔 하는 조작뿐.** 한 화면에 들어오게.

- 맨 위(`Hero`): 카드로 더 써도 되는 돈(큰 숫자) + 하루 몫 + 작은 두 줄(월급만으로 · 지금 통장으로 다 내면).
- '한눈에' 목록(`AssetList`, 늘 펼침): 통장(통장별·은행과 맞춘 시각) + 다음 달 월급 − 안 낸 카드값
  (카드별·할부 포함) − 이번 달 남은 고정지출(이름까지) − 다음 달 고정지출(통장·카드) = 카드로 더 써도
  되는 돈. **목록 순서와 부호가 곧 식**이라 '계산 보기'가 따로 없다.
- 조작 버튼 셋: 입출금·맞추기 / 카드 결제·명세서 / 고정지출 처리(남은 건수). 누르면 그 칸만 편다.
- 한때 타일·'계산 보기'로 접어 둔 판(같은 날)이 있었는데, 숫자를 보려고 눌러야 해서 바로 바꿨다.
- **설정의 통장·카드 관리는 이름만**(2026-09-14). 예전엔 카드 옆에 일시불만(bill) 찍혀서 홈의 할부 포함
  금액과 달랐고(현대 1,314,856 vs 1,411,274, 롯데 3,993 vs 853,713), '조정'(카드값 수동 추가·초기화)은
  맞추기·명세서·결제 확인과 겹쳤다 → 금액·조정을 뺐다. 같은 숫자를 두 화면에 다르게 두지 말 것.
  이름엔 은행·카드사 이름이 들어가야 알림이 붙는다고 안내한다.
- **새 버전은 알아서 바로 쓴다**(`main.jsx`): 화면으로 돌아올 때마다 새 버전을 묻고, 서비스워커가
  바뀌면 화면을 한 번 새로 고친다(입력 중이면 끝날 때까지 기다림). 예전엔 앱을 두 번 열어야 했다.

## 이 앱을 만든 이유 — 다음 달 월급 (2026-09-14)

사용자 말: **"다음 달 월급 들어온다는 전제하에 쓰고 있는데 카드값이 다음 달 월급 들어오는
걸 넘어서지 않게 할려고 이 앱을 만든 거라서."** 새 기능·숫자는 전부 이 질문에 맞출 것.

- 월급은 **말일~다음 달 3일** 사이에 들어온다. 금액은 거의 같다(설정의 `monthlyPay`).
- 카드는 **1일~말일 사용분이 다음 달 청구**된다. 그 청구와 다음 달 고정지출을 그 월급으로 낸다.

홈 맨 위 `PayCard` — **통장까지 합쳐서**(2026-09-14 사용자 선택: "다음달 월급기준에서 현재
내통장잔고도 집어넣어야 되는거 아닌가?"):

```
카드로 더 써도 되는 돈 = 통장 잔액 (+ 월초 안 들어온 이번 달 몫 월급) + 다음 달 월급
                       − 안 낸 카드값 − 이번 달 안 나간 고정지출
                       − 다음 달 고정지출(통장 + 카드 할부·정기결제)
                     = 아래 '지금 통장 기준' 잔여금액 + 다음 달 월급 − 다음 달 고정지출
하루 몫 = 위 ÷ 이번 달 말일까지(오늘 포함)
```

- 통장을 넣으려면 통장에서 먼저 나갈 돈(안 낸 카드값, 이번 달 안 나간 고정지출)도 같이 빼야
  맞는다. 그래서 두 카드 숫자가 맞물린다(위 = 아래 + 월급 − 다음 달 고정지출).
- **다음 달 카드 고정지출도 뺀다**(사용자 지적 — 처음엔 청구가 그다음 달이라 뺐다가 빠뜨렸다).
  다음 달에 어차피 긁히는 돈이다. 이번 달 카드 사용분은 안 낸 카드값에 이미 있어 또 안 뺀다.
- 월급이 이미 들어왔으면(payIn) 통장 잔액에 있으니 '통장 잔액에 포함' 0원.
- 처음 목적 그대로의 **'통장 없이 월급만으로'**(`payLeft`)는 맨 아래 작은 줄:

```
월급만으로 = 곧 들어올 월급 − 다음 달 통장 고정지출 − 이번 달 카드값
이번 달 카드값 = 이번 달 일시불(정기결제 카드반영 기록 제외) + 이번 달 카드 할부 몫 + 이번 달 카드 정기결제
```

- **카드 `bill`을 쓰지 않는다.** bill은 '결제하기'까지 쌓이는 한 칸이라 어느 달 사용분인지
  모른다(14일에 누르면 그달 1~13일 것까지 0이 된다). 날짜로 센다.
- 월급 입금 알아보기(`findPayDeposit`): 이 달 25일~다음 달 5일의 입금 중 적요 '급여·월급·상여'
  또는 적어 둔 월급 ±30%. 찾으면 그 금액으로('M/D 들어옴'), 못 찾으면 적어 둔 금액('예상').
- 월급을 안 적었으면 식 대신 적으라고 안내한다. 목표 지출액에서 옮겨 오지 않는다.
- 하루 몫은 이 카드에만 둔다. 한 화면에 '하루 N원'이 둘이면 어느 걸 따를지 모른다.

**통장 기준도 같은 크기로 둔다**(사용자 선택 — "그대로 크게 둘 다"). 사용자가 적자를 안 내려는
것이기도 해서다 — "통장금액보다 카드값이 더 높지만 않으면 되거든." 이 식은 2026-09-13에
사용자가 직접 제안했다("실시간으로 통장-(이번달 카드값+고정지출)=잔여금액"). 링 게이지와
'이번 달 운용 가능'은 홈에서 뺐다.

```
잔여금액 = 통장 잔액 (+ 월초 5일까지 아직 안 들어온 이번 달 몫 월급) − ( 안 낸 카드값 + 안 나간 고정지출 )
```

- 한때 이 카드 끝에 '월급 들어온 뒤 여유'를 이어 붙였다가(2026-09-14), 맨 위 카드로 합쳤다.
- **'월급 전까지'라고 부르지 않는다.** 안 낸 카드값(bill)엔 월급 뒤에 나갈 이번 달 사용분도
  들어 있다. 제목은 '나갈 돈을 지금 통장으로 다 내면'.

- 세 줄을 식 그대로 보여 준다(`LeftoverCard`). 결과만 있으면 왜 그 숫자인지 모른다.
- **안 낸 카드값** = `cardBillTotal`(bill + 이번 달 할부 몫 − 결제하기로 낸 할부). 지난달 걸
  아직 안 냈으면 그것도 들어간다 — 그래서 라벨이 '이번 달 카드값'이 아니다.
- **안 나간 고정지출** = `unpaidFixedSum`(처리 안 한 것만). 나간 건 통장 잔액에 이미 빠져 있다.
- 하루 몫(`하루 N원씩 쓰면 맞아요`) = 잔여금액 ÷ 오늘 포함 남은 날. 모자라면 안 보인다.
- 통장을 한 번도 안 넣었으면(잔액 0, 은행과 맞춘 적 없음) 잔액부터 맞추라고 안내한다.
- 통장 잔액이 틀리면 이 숫자가 다 틀린다. 그래서 은행 알림의 잔액으로 잔액을 맞춘다
  (아래 자동 등록 참고).
- 목표 지출액(`spendingGoal`)과 위의 목표 기준 여유 계산(`remaining`)은 ctx에 남아 있지만
  아무 화면도 안 쓴다. 설정·온보딩의 그 칸은 2026-09-14에 '월급(실수령)'으로 바뀌었다.

## 안 내고 넘어가는 달 · 언제 나가고 언제 맞춘 숫자인지 (2026-09-19)

- **고정지출 '안 냄'**(`skipMonths`) — 사용자: "모임회비 같은 건 가끔 여유가 없어서 모른척 넘어갈 때도
  있어. 안 내고 다음 달 되면 그대로 패스." 누른 달은 **계산에서 통째로 뺀다**(남은 고정지출·여유).
  달별로 적으므로 다음 달엔 저절로 다시 나오고, **못 낸 돈이 쌓이지 않는다.** 할부에는 안 붙인다.
- **카드에 결제일**(`payDay`) — 홈에 "10/12 결제 · 23일 뒤", 3일 안이면 경고색. 설정 → 카드 관리에서
  적거나 `drop-send --payday 12`로 넣는다(금액 없이 결제일만 보낼 수 있다).
- **카드값을 카드 앱과 언제 맞췄는지**(`syncedAtMs`, 명세서 맞추기·cardbill이 적는다) — 일주일 넘으면
  경고색. 카드값은 결제 확인·명세서·맞추기가 같은 한 칸을 덮어써서 **어긋나도 조용하다**(9/17에
  552,948원이 어긋나 있었다). 통장의 '2시간 전 맞춤'과 같은 역할이다.
- **하루 몫 신호등** — 남의 기준이 아니라 **내가 이번 달 실제로 쓴 하루 평균**의 60% 아래면 "빠듯해요".
- **남은 고정지출에 날짜** — 자동이체일(`autoPayDay`)이 있으면 그 날짜, 없으면 **지난달에 며칠에
  나갔는지**(paidMonths가 가리키는 기록의 날짜)로 짐작해 적는다. 날짜가 지났으면 빨갛게.
  자동이체일이 적힌 것은 그날 앱이 알아서 처리하므로 목록에 안 남는다 — 남는 건 날짜를 모르는 것들이다.
- **대리결제로 돌려받을 돈**은 카드값 아래 설명 한 줄로. 식(부호 붙은 줄)은 안 흔든다.

## 자동 등록 (2026-09-08)

안드로이드 껍데기가 카드·은행 알림을 읽어 넘기면 `lib/auto-record.js`가 처리한다.

- **카드 승인** → 지출 기록 (여유에 반영)
- **은행 입출금** → 통장 입출금 기록 (잔고만)
- 애매하면 넣지 않고 기록 탭의 알림함에 남긴다.
  **놓친 건 나중에 넣으면 되지만 잘못 들어간 건 찾기 어렵다.**
- **알림함은 휴대폰에 저장된다**(`INBOX_KEY`, 가계부 데이터와 다른 칸). 껍데기는 웹이
  가져가는 순간 큐를 비우므로, 예전엔 보류된 알림이 앱을 닫으면 어디에도 안 남았다.
- **한 번 보류한 것은 다시 자동으로 판단하지 않는다.** 그러지 않으면 미뤄 둔 것이
  나중에 엉뚱한 데이터 변경에 편승해 들어간다(백업 복원 때 실제로 그랬다).
- 넣은 것에는 `auto: true` — 내역에서 '자동' 배지로 보인다.
- **사용자는 "확인 안 누르고 전부 자동으로"를 원한다**(2026-09-11). 알림함에 남는 건
  어느 카드·통장인지 정말 모를 때와 금액을 못 읽을 때뿐이어야 한다. 새 규칙을 더할 때
  '애매하면 보류'보다 '제대로 판단해서 처리'를 먼저 찾을 것.
- **같은 거래인지는 결제 시각으로 가른다.** 자동 기록엔 문자의 시각을 `autoTime`으로
  남긴다. 같은 날·같은 금액이어도 시각이 다르면 다른 거래(지하철 두 번), 같으면 같은
  거래(앱 알림과 문자로 두 번 온 것). 손으로 적은 것·앱이 만든 출금은 시각이 없어
  같은 날·같은 금액이면 같은 거래로 보고 **넘긴다**(보류 아님, 알림 문구로 알림).
- **먼저 적힌 기록은 알림으로 바로잡는다.** 손으로·앱이 적은 기록과 같은 거래면 새로
  안 넣고 그 기록을 은행이 알려 준 날짜·통장으로 고친 뒤 `bankConfirmed`. 확인된 건
  다시 짝이 안 된다(같은 금액이 진짜 두 번이면 두 번째는 기록). 다른 통장으로 옮기는 건
  카드값 '결제하기' 기록만(첫 통장에 짐작으로 적으므로).
- **알림으로 들어온 기록(auto)은 은행이 알려 준 진실이다.** 버튼을 누르면 새로 만들지
  말고 거기에 잇고, 연결을 풀거나 지출을 지우·고칠 때도 그 기록은 지우지 않는다.
- 이미 받아 처리한 알림 문구는 `SEEN_KEY`에 남겨 다시 안 받는다(재시작 시 껍데기가
  알림창을 다시 훑어 넘기므로).
- **결제했다가 취소하면**: 둘 다 기록 전이면(알림함 포함) 서로 지운다. 이미 들어갔으면
  그 기록을 빼고 카드값을 되돌린다 — 자동 기록을 먼저, 없으면 손으로 적은 것을.
  이번 달·정산 안 붙인 것만. 자동 등록한 할부의 취소면 그 할부 항목을 지운다.
- **할부 결제는 '할부(고정지출)'로 자동 등록한다**(월 금액 = 총액 ÷ 개월, 나머지는
  첫 달). 지출로 넣으면 총액이 이번 달 카드값에 통째로 잡힌다. 개월 수를 못 읽으면 보류.
- 카드 문자 끝의 '누적○○원'은 결제를 막지 않는다(현대카드는 늘 붙는다). 대신 '결제
  예정·결제 금액·결제일·청구·이용대금'은 결제로 안 본다. **'예정'·'안내' 한 낱말로는 거르지
  않는다**(2026-09-14) — 현대카드 앱 푸시의 "승인 안내", "적립 예정 88M"이 걸러질 수 있었다.
- **카드사가 보내는 결제 말고 다른 문자**(2026-09-14, 사용자가 현대카드 문자함을 보여 줌):
  · 명세서·결제금액 안내("09/12 결제금액 1,452,107원(09/01기준)") → 조용히 넘김(`INFO_NOTICE`).
  · **카드값 결제 확인**("[현대 코스트코] 김*혁님 09/02 841,510원이 입금되었습니다") → 통장 입금이
    아니다. 그 카드의 카드값을 **이번 달 기록 합**으로 맞추고 `paidAtMs`(1일~말일 사용분이 다음 달
    청구라서, 결제가 확인되면 남는 건 이번 달 사용분). 나눠 내서 두 번 와도 같다. 보낸 곳이 카드사로
    가려지고, 그 이름의 통장이 없고, '잔액'이 없을 때만. **지난달 날짜면 넘긴다**(늦게 읽힌 것).
  · **대중교통 한 달 합계**("08월 버스+지하철 이용금액은 총 48,600원") → 그 달 `linkedTransitMonth`
    기록을 그 금액으로(없으면 그 달 말일로 새로). 사용자 설명: **카드값을 1일에 내고 1~2일 뒤에
    '방금 낸 청구에 들어 있던' 대중교통 금액을 알려 준다** → 이번 달에 그 카드 결제 확인이 이미
    있으면 카드값은 안 건드리고 기록만. 아직 안 냈으면 카드값에 더한다.
  · 몇 달 전 결제의 취소("03/12 … 641,340원 취소처리") → 알림함(어느 청구에서 빠질지 몰라서).
- **현대카드는 앱 푸시로 받는다**(2026-09-14, 사용자와 합의). 차선생·뱅크샐러드 같은 연동은
  마이데이터 허가 사업자만 할 수 있어 개인 앱은 못 한다. 푸시는 리스너가 이미 읽는다(보낸 앱
  com.hyundaicard.* → 현대). 하이패스 통행료처럼 알림이 없는 건 월 1회 명세서 붙여넣기로.
  그래도 빠지는 게 많으면 다음 단계는 접근성 서비스로 카드 앱 이용내역 화면 읽기(실기 시험 필요).
- **어느 은행·카드인지는 문구가 아니라 보낸 앱(패키지)으로 가린다.** 은행 이름이
  문구에 아예 없는 형식이 흔하다(KB스타뱅킹). 보낸 곳을 아는데 그 이름의 통장이
  없으면 글자로 찍지 않는다 — 문구에 든 남의 회사 이름에 붙어 버린다.
- **문자는 맨 앞의 보낸 곳 이름으로 가린다**(`issuerOfHead`, 2026-09-13). 문자 앱이 보낸
  것이라 패키지로 못 가리는데, 예전엔 이름 두 글자를 문구 아무 데서나 찾아서 가맹점
  '롯데마트'가 롯데카드에 붙었고, 하나뿐이면 아예 안 보고 붙여서 KB국민체크 문자가
  현대카드에, 기업은행 출금이 국민은행에 붙었다(잔액 맞춤까지 겹치면 국민은행 잔액이
  기업은행 잔액으로 덮인다). '[Web발신]'과 앞 전화번호를 걷어 내고 앞 12자에서 찾는다.
- 문자로 온 '결제'는 '승인·카드·체크'가 있어야 카드 결제다(`cardApproval`). 간편결제의
  "결제 완료"는 포인트일 수 있고, 카드로 냈으면 카드사 문자가 따로 온다.
- 알림은 앱을 다시 열 때와 **켜 둔 동안 15초마다** 가져온다. 예전엔 '돌아옴'
  신호에만 기대서, 앱을 보는 중에 온 알림은 나갔다 와야 들어왔다.
- 고정지출과 짝이 맞으면 그 항목을 **처리 완료로 표시**한다(`paidMonths`).
  버튼이 사라지므로 며칠 뒤에 눌러도 중복이 안 생긴다.
  적는 달은 '지금'이 아니라 **그 결제가 일어난 달**이다 — 12/31 알림을 1/1에
  읽으면 양쪽 달이 다 틀어진다.
- **은행 알림의 잔액으로 통장 잔액을 맞춘다**(2026-09-13). "잔액1,392,375"가 은행이
  알려 준 진짜 잔액이다. 앱 잔액과 다르면 차액만큼 '은행 잔액에 맞춤'(`isAdjustment`,
  `auto`)을 넣고 통장에 `bankSync: {at, balance}`를 남긴다. 알림을 놓쳤든 '결제하기'를
  먼저 눌렀든 다음 은행 알림 한 번에 다시 맞는다. **`bankSync.at`보다 앞선 거래 알림은
  잔액을 안 흔든다** — 늦게 와서 새로 기록됐으면 같은 금액을 되돌려 둔다(이미 마지막
  잔액에 들어 있던 거래다). 시각이 없는 알림으로는 안 맞춘다. 사람이 '맞추기'를 눌러도
  `bankSync`를 남긴다.
- **같은 거래가 두 길로 오면 받는 자리에서 거른다**(`dealKey` = 날짜·시각·금액·종류,
  `SEEN_DEAL_KEY`). 문자함을 직접 읽으면(껍데기 1.2) 같은 문자가 문자 앱 알림(보낸 사람
  + 본문)과 문자함(본문)으로 글자가 다르게 온다. 결제는 시각으로 걸러지지만, 취소는 두
  번째 것이 알림함에 뜨고 보류된 건 두 줄이 된다. 먼저 온 쪽이 알림함에 보류돼 있으면
  나중 것을 버리지 않고 **바꿔서 다시 판단**한다(문자는 보류돼도 은행 앱 알림은 가려진다).
- 잔액 맞춤(`isAdjustment`)도 `auto`다. '이미 들어온 자동 기록에 잇기'(출금처리·현금
  지출·결제하기)와 내역의 입출금 합계에서는 반드시 뺀다 — 거래가 아니다.
- '(광고)' 문자는 결제로 안 본다 — 금액과 '결제'가 다 있어서 카드가 하나면 들어갈 수 있었다.
- 금액이 같은 고정지출이 여럿이면 문구의 적요로 가르고, 못 가르면 보류한다.
- **짝은 카드매달반복만.** 카드 할부는 이미 `fixedPortion`으로 카드값에 들어가
  있어서, 짝을 지어 `bill`에 또 더하면 그 달 카드값이 두 배가 된다. 할부 회차와
  금액이 같은 승인은 아예 자동으로 안 넣는다(알림함으로).

## 카드 명세서로 맞추기 (2026-09-14, `lib/statement.js`)

알림이 없던 결제(하이패스 통행료, 자동 등록 전의 결제)와 '맞추기'로 금액만 넣은 카드값 때문에
날짜로 센 '이번 달 카드값'이 실제보다 적었다. 홈 카드 '맞추기' 안에서 카드 앱 '결제 예정'
이용내역을 한 줄씩("09/13 가맹점 86,491") 붙여 넣으면:

- 같은 금액·날짜 ±2일이면 앱 기록과 짝. 정기결제 '카드반영' 기록은 '완료'를 누른 날로 적혀
  있어서 같은 달·같은 금액이면 짝으로 본다(안 그러면 유튜브가 두 번 들어간다).
- 없는 건 새로 넣는다(`fromStatement`, 내역에 '명세서'). 통행료·택시는 '교통', 그 밖엔 미분류.
  카드 정기결제와 금액이 같으면 가맹점에 그 이름 조각이 들어 있을 때만 카드반영으로 잇는다.
- **할부 줄("할부(4/12) 96,418")로 앱 할부 금액을 그 달 명세서 금액으로 고친다**(`overrides`,
  지난 달은 안 건드림). 유이자 할부는 수수료가 남은 원금에 붙어 매달 줄어드는데 앱엔 첫 달 금액이
  매달 그대로였다. 줄에 '이용금액·수수료'가 있으면 남은 회차도 계산해 넣는다(원금 균등, 수수료율 =
  이번 수수료 ÷ 이번 회차 전 남은 원금). 같은 카드·같은 개월 수·같은 회차로 찾고, 못 찾으면 알린다.
- 명세서 기간인데 명세서에 없는 앱 기록은 **지우지 않고** 목록으로 보여 준다.
- 카드값 = 명세서 일시불 + 명세서 뒤 앱 기록 + 아직 청구 전 카드반영. **그 전 청구는 낸 것으로 본다**
  (붙여 넣는 건 다음 결제 예정 내역이라서). `paidAtMs`도 그 시각으로.
- 미리 보기를 보여 주고 '이대로 적용'을 눌러야 바뀐다.
- 사용자 내역을 코드나 배포물에 넣지 않는다 — 배포 주소는 공개다. 대신 사용자가 붙여 넣는다.

## Claude가 대신 넣기 — 암호화된 받은편지함 (2026-09-14, `lib/drop.js`, `tools/drop-send.mjs`)

사용자: "적어 주면 내가 옮겨서 등록이 아니라, 내가 보여 주면 너가 알아서 그냥 쭉 끝까지 입력해 놔 줘."
데이터는 휴대폰에만 있고 배포 주소는 공개라서:

1. 앱이 무작위 **받기 코드**(20자)를 만들어 설정 → 'Claude가 대신 넣기'에 보여 준다. 사용자가 한 번 알려 준다.
2. 캡처를 받으면 명세서 줄("09/13 가맹점 86,491")로 옮겨 적고 **합계가 캡처의 총액과 원 단위까지 같은지
   확인한 뒤**(할부 줄엔 '이용금액 … 수수료 …'까지):
   ```
   node tools/drop-send.mjs <받기코드> <카드이름조각 예: 롯데> <명세서.txt>
   ```
   → `public/drops/<코드 해시>.json`에 AES-GCM(키 = 코드 PBKDF2 20만 번)으로 잠가 넣는다. 커밋·푸시로 배포.
3. 앱은 켤 때·돌아올 때 받아 풀고, 아직 안 적용한 것만 명세서로 맞추기(`reconcileStatement`)를 돌린다.
   카드는 이름 조각이 든 카드가 딱 하나일 때만. 결과는 알림 문구와 설정의 적용 기록에 남는다.

- **롯데카드는 할부를 한 달 일찍 낸다**(2026-09-15, 사용자: "원래대로 결제하면 원금은 같은데 이자가 한 달치
  더 붙어서, 한 달 일찍 내면 원금 + 이자 절반 정도만 나온다"). 쏘렌토(기아화성) 할부 44,600,000원 60개월,
  청구원금 743,300 + 수수료(남은 원금 × 율). 9/15에 10/14 결제분(29/60, 850,367원)을 미리 냈다.
  → 명세서를 `--prepaid`로 보내면: 그 달 할부 몫을 낸 것으로(`installPaid`), 카드에 `earlyPay`, 남은 회차는
  청구원금 + 남은 원금 × 하루 율 × **그 회차 기간 날수**로 계산(사용자: "매달 일수 28~31일에 따라 오르내린다" —
  한 달 일찍 내는 카드는 앞 달 날수, 제때 내는 카드는 그 달 날수. 마지막 60/60은 2029-04). 다음 달부터는 롯데 결제 확인 문자가 오면
  그 달 할부를 낸 것으로 처리한다. '월급만으로'에선 롯데 할부를 이번 달 대신 다음 달 몫으로 센다.
- **롯데를 더 자동으로**(2026-09-16, "롯데카드도 좀 더 자동으로 할 수 있을 법한 게 뭐가 있을까?"):
  · 롯데카드 앱(LOCA, 패키지 `com.lcacApp`) 알림도 롯데로 알아본다.
  · **통장 출금 알림에 '○○카드'가 찍히면 그 카드 결제**로 본다(`cardNamedIn` → `settleCard`) — 기업은행
    "[출금] 850,367원 롯데카드", KB "현대카드". 카드사 문자가 없어도 된다. 등록한 카드 중 딱 한 장일 때만.
  · 한 달 일찍 내는 카드는 **낸 금액 − 이번 달 일시불 = 그 달 할부 실제 금액**으로 보고 그 달 금액을 바꾸고
    남은 회차를 새 하루 율로 다시 계산(`reprojectInstallment`, 바탕은 명세서 맞출 때 남긴 `instPlan`).
    예상과 15% 넘게 다르면 금액은 안 바꾸고 낸 것으로만. → 매달 캡처 없이 스스로 맞아 간다.
  · **일부 결제는 낸 만큼만 뺀다**(2026-09-17, 사용자: "현대카드 일부를 먼저 결제했는데 인식을 제대로 못했어").
    예전엔 결제 확인이 오면 무조건 카드값을 '이번 달 사용분'으로 다시 잡아서, 일부만 내면 남은 빚이 사라졌다.
    청구돼 있던 몫(카드값 − 이번 달 사용분)과 낸 금액이 1만 원·5% 안에서 비슷하면 전액 결제로 보고 예전처럼
    다시 잡고, 아니면 `카드값 − 낸 금액`. 한 달 일찍 내는 카드(롯데)는 청구액을 통째로 미리 내므로 늘 전액.
  · **현대카드 앱의 '일부결제금액이월약정(리볼빙)'은 안 쓰는 것이다**(2026-09-17 사용자 확인 — "광고식으로
    표시된 거야, 이용한 적 한 번도 없어"). 결제 세부에 금액이 찍혀 있어도 총 결제 금액이 전액이면 이월이 없다.
    계산을 바꾸지 말 것. 혹시 실제로 일부만 빠지면 아래 '일부 결제' 규칙이 알아서 맞춘다.
  · **같은 결제를 두 길로 받아도 한 번만**(`paidLog`, 36시간·같은 금액). 카드사 문자와 통장 출금 알림이 둘 다
    온다. 예전 방식(다시 잡기)은 두 번 해도 같았지만, 빼기는 두 번 하면 카드값이 반으로 준다.
  · 통장 출금 문구가 "코스트코현대 오픈뱅킹출금"이라 '현대카드'로는 못 찾았다 → **카드 이름의 낱말이 둘 이상**
    찍혀 있어도 그 카드로 본다(낱말 하나는 가맹점 이름에 붙는다).
  · 결제 확인 말을 좁혔다: '입금되었습니다·즉시결제/선결제 완료·카드대금 출금'. 그냥 '결제 완료'는
    카드 앱 승인 알림에도 쓰여서 카드값을 엉뚱하게 비울 수 있었다. '승인'이 든 문구는 결제 확인이 아니다.
- **시험이 받은편지함 폴더를 통째로 지우지 않게 할 것**(2026-09-16 실제로 사용자 파일이 지워졌다가 git에서 되살림).
  시험용 코드(TESTABCDEFGHJKLMN234)의 파일 `c9f9e6368f4631dd950c5896.json`만 지운다.
- **받기 코드를 저장소·문서·커밋에 적지 말 것.** 이 PC의 `C:\Users\Pass1\.claude\passbook-drop-code.txt`에만 둔다
  (다음 세션용). 사용자가 코드를 새로 만들면(앱 데이터 초기화 등) 그 파일도 바꾼다.
- **카드값만 카드 앱 숫자로 맞추기**(`kind: "cardbill"`, 2026-09-17):
  `node tools/drop-send.mjs <코드> <카드이름조각> --bill <금액> [메모] [--install 이름:YYYY-MM:금액]`
  명세서 줄을 다 옮기지 않고 카드 앱의 '결제 예정 금액'으로 카드값을 그대로 맞춘다. 일부 결제·리볼빙처럼
  우리 계산이 못 따라가는 경우에 쓴다. **할부 몫은 빼고 넣는다** — 할부는 고정지출로 따로 세므로 넣으면 두 번 잡힌다.
- **알림이 안 오는 교통비는 '그 달 한 줄'**(2026-09-17, 사용자와 합의 — 대중교통·하이패스):
  `--row 하이패스:2026-09:59440:교통` (여러 번 가능). 건수는 수십 건인데 알림이 없고 금액이 달마다 다르다.
  같은 달 줄(`linkedMonthly` = `이름:YYYY-MM`)이 있으면 새로 안 만들고 **금액만 갱신**한다. 대중교통은 이미
  같은 방식(`linkedTransitMonth`, 카드사 합계 문자로 자동). **이 줄들은 카드값을 안 건드린다** — 카드값은
  `--bill`로 카드 앱 숫자에 맞춘 값이라 여기서 더하면 두 번 잡힌다. 지출 기록·카테고리(교통비가 한 달에
  얼마인지)용이고, 다음 달 카드 앱 숫자를 맞출 때 그 달 줄도 같이 보낸다.
- 한 파일에 최근 20건까지. 같은 명세서를 두 번 보내도 맞추기는 두 번 해도 같다.

## 위젯 · 아침 알림 · 초과 경고 (2026-09-17, 껍데기 1.3)

사용자가 고른 다음 두 가지다 — "앱을 안 열어도 보이게"와 "넘으면 그때 바로".

- **숫자는 웹이 낸다.** 홈 맨 위 계산을 `App.jsx`의 ctx로 올렸다(`bankLeft`, `canSpend`,
  `daysLeft`, `perDay`, `bankKnown`, `hasPay`). 화면과 위젯이 각자 계산하면 반드시 갈라진다.
  `NativeSummary`(화면 없는 컴포넌트)가 바뀔 때마다 `pushSummary()`로 껍데기에 넘긴다.
  **게이트(로딩·온보딩) 뒤에서 useEffect를 쓰면 훅 수가 달라져 앱이 죽는다**(React #310) —
  그래서 효과가 아니라 컴포넌트다.
- 껍데기(`Summary.kt`)는 받은 값을 **글자로 옮기기만 한다.** 홈 화면 위젯(`SummaryWidget`),
  매일 아침 8시 알림(`Daily`, `setInexactRepeating` — 정확한 알람은 12부터 권한이 필요하고
  몇 분 늦어도 상관없다), 그리고 `canSpend < 0`이 되는 순간 경고.
- **경고는 3만 원 더 나빠질 때마다만 다시 띄운다.** 결제마다 울리면 알림을 꺼 버리고,
  그러면 정작 필요할 때 안 보인다. 플러스로 돌아오면 기억을 지운다.
- **낡은 숫자는 낡았다고 적는다.** 알림은 앱이 열릴 때 처리되므로 며칠 안 열면 요약도 그만큼
  낡는다 → 위젯·알림에 '(N일 전 기준)'. 위젯은 WebView의 localStorage를 직접 못 읽는다.
- 알림 권한(13+)은 앱을 열 때 한 번만 묻고, 거절해도 위젯은 그대로 보인다. 위젯은 앱이 못
  붙인다 — 홈 화면에서 사람이 꺼내야 해서 설정에 그 방법을 적어 뒀다(붙은 개수도 보여 준다).

## 안드로이드 껍데기

`D:\passbook-android` (별도 폴더, 이 저장소 밖).

웹앱은 알림을 읽을 수 없어서(권한이 아니라 기능이 없다) 그것만 네이티브로 뺐다.
화면은 하나도 새로 만들지 않는다 — WebView로 이 앱을 띄운다.

```
D:\dev\jdk17          JDK
D:\dev\android-sdk    SDK
D:\dev\gradle-8.7     Gradle

빌드:  JAVA_HOME=D:\dev\jdk17  gradle assembleRelease --no-daemon
결과:  app\build\outputs\apk\release\app-release.apk
```

**서명 키를 잃으면 업데이트를 덮어 설치할 수 없다.** 지웠다 깔아야 하고 그러면
앱 안의 기록이 날아간다. 키: `E:\클로드백업\passbook.jks` (비밀번호 `passbook`).

웹만 고쳤으면 **APK를 다시 깔 필요가 없다** — 원격 주소를 띄우므로 푸시하면
앱을 다시 켤 때 반영된다. 네이티브(알림 규칙·백업·앱 이름)를 고칠 때만 재설치.

**껍데기 1.1(2026-09-12)** — 알림 리스너가 끊겼는지(`connected`) 들고 있다가 앱을 열 때
끊겨 있으면 다시 잇는다(`requestRebind`). 삼성은 배터리 관리로 리스너를 자주 끊고, 끊겨도
권한은 '켜짐'이라 사람이 모른다. 대화형 문자 알림은 `android.messages`를 한 통씩 읽는다.
진단(`diagnostics()`): 연결 여부·배터리 최적화·마지막으로 본 알림·금액 든 알림을 결제로
봤는지 최근 30건. 웹은 받은 알림마다 처리 결과를 `ALERT_LOG_KEY`에 남기고, 설정 →
'최근 결제 알림'에 둘을 이어 보여 준다 — **결제가 안 들어왔다는 말을 들으면 이 화면부터
보여 달라고 할 것.** 껍데기에서 걸러졌는지, 앱으로 안 왔는지, 웹이 넘겼는지 바로 갈린다.
웹 처리 기록이 없으면 대기 건수와 실제 내역을 보고 '아직 안 가져옴 / 내역에 있음 /
가져왔지만 내역에 없음'으로 가른다 — 기록이 없다고 '안 가져왔다'고 하지 않는다.

**껍데기 1.2(2026-09-13)** — 문자함을 직접 읽는다(`SmsInbox.kt`, `READ_SMS`). 웹이
알림을 가져갈 때마다(`pullPending`) 지난번 이후 온 문자(SMS)와 긴 문자(MMS — 카드 결제
문자가 대개 이렇다)를 훑어 결제 문자를 같은 큐에 넣는다. 알림을 놓쳐도 문자함엔 남아
있어서다. 처음엔 어제 0시부터. 권한은 설정 → '문자 읽기 허용'으로 묻고, 거절하거나
창이 안 뜨면(제한된 설정) 앱 정보 화면을 연다. 은행 앱 푸시(KB스타뱅킹 등)는 문자가
아니라서 여전히 알림 리스너로만 받는다.

자동 백업은 껍데기가 한다 — 화면을 벗어날 때 `Download/내돈챙겨줘/`에 하루
한 파일, 30일치. 앱 전용 폴더가 아닌 이유는 앱을 지워도 남아야 하기 때문이다.
되돌리는 건 설정 → 데이터 백업 → '휴대폰에 저장된 백업'에서 한다.
