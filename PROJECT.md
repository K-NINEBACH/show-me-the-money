# 내 돈 챙겨줘 — 프로젝트 인수인계 문서

개인 가계부 PWA. 급여 후 과소비 방지가 목적. 서버 없이 브라우저 localStorage에만 저장.

2026-09-08부터 안드로이드 껍데기 앱이 하나 붙어 있음(별도 폴더 `D:\passbook-android`).
알림 읽기와 자동 백업만 맡고 화면은 이 웹앱을 그대로 띄움 — **§10** 참고.

---

## 1. 기술 스택 / 실행

- React 18 + Vite 5, 런타임 의존성은 `lucide-react`(아이콘)뿐. **차트 라이브러리 없음**(recharts 제거됨)
- 빌드 전용(devDependencies)으로 `vite-plugin-pwa`가 있음 — 2026-08-23에 서비스워커
  오프라인 프리캐시용으로 추가(아래 "체감 속도 최적화" 참고). 브라우저로 나가는 코드에는
  전혀 안 실림, `npm run build`할 때만 관여함.
- 스타일링 라이브러리 없이 **인라인 style 객체만** 사용
- **2026-08-21 정리**: 혼자 쓰는 개인용 도구로 확정하면서 안 쓰던 기능(PIN 잠금·휴지통 복원·카테고리별 예산)을 걷어내고, 단일 2,800줄 `App.jsx`를 화면/로직별 여러 파일로 분리했음. `git log`에서 그 커밋 메시지로 찾아보면 변경 배경이 나옴.

```
npm install
npm run dev      # 개발
npm run build    # 배포 빌드 → dist/
```

### 파일 구조
```
passbook-app/
├── index.html
├── package.json
├── vite.config.js
├── public/
│   ├── manifest.webmanifest   # PWA 설정 (앱 이름/아이콘/테마색)
│   ├── sw.js                  # 서비스워커 소스 (빌드 시 vite-plugin-pwa가 dist/sw.js로
│   │                            변환 — public/sw.js를 직접 배포하는 게 아님, 아래 참고)
│   └── icons/icon-192.png, icon-512.png
└── src/
    ├── main.jsx
    ├── App.jsx                 # 셸: 로딩/온보딩/월마감 게이트 + 하단 탭 라우팅 + ctx 계산
    ├── lib/
    │   ├── constants.js         # STORAGE_KEY, PALETTE, QUICK_AMOUNTS (의존성 없음)
    │   ├── theme.js              # THEMES, useTheme, F, paperCard/inputSty/primaryBtn
    │   ├── data.js                # defaultData, migrate, 날짜·금액 유틸, netAmount, fixedInfo (React 없음)
    │   ├── native.js              # 안드로이드 껍데기와의 다리 — 껍데기가 없으면 조용히 넘어감
    │   └── auto-record.js         # 결제 알림을 확인 없이 기록으로 넣는 규칙 (2026-09-08)
    ├── components/
    │   ├── common.jsx            # MoneyInput, Field, SectionLabel, NavBtn 등 여러 화면 공용 조각
    │   ├── payment-inbox.jsx     # 자동으로 못 넣은 알림을 사람에게 보여주는 자리 (기록 탭 맨 위)
    │   └── ErrorBoundary.jsx
    └── screens/
        ├── Gates.jsx              # Onboarding, MonthWrapUp (탭 밖의 전체화면)
        ├── Home.jsx, Add.jsx, Ledger.jsx, Calendar.jsx, Settings.jsx
```
화면을 고칠 땐 대부분 `screens/` 안의 해당 파일만 보면 됨. 여러 화면이 같이 걸리는 계산(카드값·예산 등)은 `App.jsx`의 `ctx` 구성부에 있음.

### 2026-08-23: 체감 속도 최적화
사용자가 "가볍다고 느껴지게" 요청해서 세 군데를 고침 — 전부 기능은 그대로, 속도만:
- **서비스워커(`public/sw.js`) 캐시 전략 교체**: 예전엔 모든 요청이 네트워크 우선이라, 내용이
  안 바뀐 JS 파일(Vite가 내보내는 `/assets/*.js`는 파일명 자체에 콘텐츠 해시가 붙어서
  내용이 같으면 파일명도 항상 같음)도 앱을 켤 때마다 매번 새로 받아왔음. 이제 해시 붙은
  `/assets/` 파일은 **캐시 우선**(있으면 즉시 응답, 네트워크 요청은 백그라운드로만) —
  체감 실행속도에 가장 크게 기여하는 변경. 캐시 이름도 v1→v2로 올리면서 `activate`에서
  옛 버전 캐시를 지우게 함 — v1은 한 번도 정리된 적이 없어서 배포할 때마다 이전 해시
  파일들이 계속 쌓이고만 있었음.
- **구글폰트 로딩 위치 이동**: 예전엔 `GoogleFonts` 컴포넌트가 React 마운트 후에
  `<style>`로 `@import`를 주입해서, 폰트 요청 자체가 JS 실행 이후로 밀렸음. `index.html`
  `<head>`에 `<link rel="preconnect">` + `<link rel="stylesheet">`로 옮겨서 HTML 파싱
  시점부터 바로 요청이 나가게 함. 전역 리셋 CSS(`box-sizing`, 스크롤바 숨김 등)도 같이
  옮기면서 `GoogleFonts` 컴포넌트 자체를 삭제(3곳에서 호출하던 것 정리).
- **자주 안 쓰는 화면 코드분할(`React.lazy`)**: 홈/기록/내역(매일 쓰는 3탭)은 그대로
  즉시 로드하고, 온보딩·월마감(`Gates.jsx`)·달력·설정은 `React.lazy` + `Suspense`로
  분리. 매일 실제로 켜서 쓰는 경로의 초기 번들이 252KB(gzip 74.9KB) →
  227KB(gzip 69.2KB)로 줄었고, 달력·설정은 별도 청크(각 6~12KB)로 빠져서 처음 그
  탭에 들어갈 때만 받아옴 — 서비스워커가 캐시해두니 그 다음부턴 이 지연도 없음.
  Playwright로 온보딩→탭 5개 전체 클릭까지 직접 구동해서 콘솔 에러·실패한 요청
  없음을 확인함.
- **폰트 굵기를 실제 사용처에 맞춤**: `index.html`의 구글폰트 URL이 실제로는 안 쓰는
  굵기를 받고 있었음 — Noto Serif KR은 코드 전체에서 거의 다 `fontWeight: 700`으로만
  쓰는데 500도 같이 받고 있었고(유일하게 굵기 명시 없던 `MonthWrapUp` 라벨은 700으로
  통일함), IBM Plex Mono(금액 숫자)는 반대로 700이 훨씬 많이 쓰이는데 500·600만 받고
  있어서 그 자리들이 살짝 더 얇은 굵기로 대체 렌더링되고 있었음. `wght@700` /
  `wght@600;700`으로 정리해서 안 쓰는 굵기 파일은 안 받고, 숫자는 코드에 적힌 굵기
  그대로 나오게 함.
- **서비스워커 설치 시점 프리캐시(`vite-plugin-pwa`, injectManifest 방식)**: 위 캐시
  전략 개선까지는 "한 번 방문해야 캐시됨" 구조라, 오프라인으로 처음 켜거나 아직 한
  번도 안 들어가본 탭(달력·설정처럼 lazy 분리된 것들)은 캐시가 없어서 못 떴음. Vite가
  빌드마다 파일명 해시를 바꾸는 탓에 `public/sw.js`에 정적으로 목록을 못 넣었는데,
  `vite-plugin-pwa`를 `strategies: "injectManifest"`로 붙여서 **빌드 시점에** 그 목록을
  `public/sw.js` 안 `self.__WB_MANIFEST` 자리에 자동으로 채워 넣게 함(`vite.config.js`).
  `manifest.webmanifest`는 원래 손으로 관리하던 것 그대로 두려고 `manifest: false`로
  플러그인이 손 안 대게 막았고, 서비스워커 등록도 `main.jsx`에서 이미 직접 하고
  있어서(`{type:"module"}` 없는 옛날 방식) `injectRegister: false` + `rollupFormat: "iife"`로
  번들 결과물이 계속 클래식 스크립트로 나오게 함. `precacheAndRoute`/`cleanupOutdatedCaches`
  (workbox-precaching)가 캐시 이름 버전 관리·정리까지 대신 해줘서, 위에서 손으로 하던
  `v1→v2` 버전 올리기 같은 관리가 이제 필요 없음. precache 목록에 없는 요청(예:
  `manifest.webmanifest`)만 `public/sw.js`의 남은 커스텀 `fetch` 핸들러가 처리.
  **검증**: `npm run build` → `vite preview`로 실제 프로덕션 빌드를 띄우고, Playwright로
  첫 방문(온라인) 후 `context.setOffline(true)`로 네트워크를 완전히 끊은 채 새로고침 →
  홈 화면 정상 렌더링 + 아직 한 번도 안 들어가본 달력·설정 탭까지 오프라인 상태로 정상
  클릭됨을 확인(콘솔 에러 없음).

### 2026-08-24: 추가 버그 훑기 + 기록 화면 실용성 개선
사용자가 "토큰 많이 써도 되니 오류·버그 잡아서 개선 + 더 심플/컴팩트하게 쓸 수 있는
방식 있으면 적용" 요청. 이번엔 `persist(` 호출을 전부 훑어서 "삭제/덮어쓰기인데 확인창이
없는 곳"을 체계적으로 찾는 방식으로 진행함.
- **`doImport`(설정 → 데이터 가져오기)에 확인창이 아예 없었음** — 앱에서 제일 되돌리기
  어려운 동작(지금 데이터를 통째로 덮어씀)인데, 카드/통장/카테고리 삭제는 전부 confirm이
  있으면서 여기만 빠져 있었음. 확인창 추가(가져올 지출 건수도 같이 보여줌) + 가져오기
  성공 후 `spendingGoalInput`이 마운트 시점 값에 멈춰있던 것도 같이 고침(새로 가져온
  목표 지출액을 입력창에 바로 반영).
- **내역 수정 폼을 연 채로 기간/카테고리 필터를 바꾸면**, 그 항목이 목록에서 잠깐
  사라졌다가 필터를 되돌렸을 때 `editingId`가 그대로 남아있어서 수정 폼이 예고 없이
  다시 열려 있는 것처럼 보였음 — 필터(`timeScope`/`category`/`search`)가 바뀌면
  `editingId`를 자동으로 닫게 함.
- **`autoProcessFixed`가 같은 달 자동이체 대상을 한 번에 여러 개 처리할 때** 전부
  `Date.now()`가 같은 밀리초라 랜덤 접미사만으론 드물게 `id`가 겹칠 수 있었음 —
  루프 인덱스를 타임스탬프에 직접 더해서 항상 겹치지 않게 함(`createdTime()`이 id의
  숫자를 그대로 이어붙여 정렬 기준으로 쓰기 때문에, 구분자를 붙이는 대신 자릿수를
  유지하는 방식으로 함 — 안 그러면 Number.MAX_SAFE_INTEGER를 넘어 최신순 정렬이 깨짐).
- **PWA를 백그라운드에 오래 두고 자정을 넘겨서 돌아오는 경우**(모바일에서 흔함),
  `today`/`curKey` 같은 값들이 마지막 렌더링 시점에 멈춰있어서 "오늘 지출"·며칠차
  표시가 어제 기준으로 보이고, 달이 바뀌었으면 월마감 화면도 안 떴음 — `visibilitychange`/
  `focus` 이벤트로 화면에 돌아올 때마다 강제로 한 번 더 렌더링해서 날짜 관련 값이 항상
  다시 계산되게 함(`App.jsx`).
- **기록(할부/고정지출) 폼도 최근 사용 카드/통장을 기본값으로**: 메인 지출 입력엔 이미
  있던 "최근 쓴 카드/카테고리 기억" 기능을, `InstallmentForm`에도 `findRecentFixedDefaults`로
  똑같이 적용 — 두 번째·세 번째 고정지출을 등록할 때도 매번 첫 카드/첫 통장부터 다시
  고를 필요 없음.
- **카드가 하나도 없으면(온보딩에서 "안 써요" 선택) 기록 화면의 "카드" 버튼 자체를
  숨김** — 예전엔 눌러도 빈 셀렉트+경고문구만 뜨고 등록이 막히는 막다른 버튼이었음.
  기본 결제수단도 카드가 없으면 "현금"으로 시작하게 바꿈. 이제 막다른 골목이 된
  경고문구도 같이 정리(죽은 코드 제거).

검증: Playwright로 카드 없이 온보딩 → 기록 화면(카드 버튼 없음 확인) → 설정에서 카드
추가 → 기록 화면(카드 버튼 다시 나타남 확인) → 데이터 내보내기/가져오기(확인창 문구
확인) → 내역 수정 폼 열고 필터 전체↔이번달 왔다갔다(자동으로 닫히는지 확인)까지 전부
직접 구동, 콘솔 에러 없음. 프로덕션 빌드 기준 오프라인 회귀 테스트도 다시 통과.

### 2026-08-24(추가): 내역 "전체 흐름" 총 지출 건수 오표시
실사용 중 발견 — 이번달·전체 흐름으로 보면 "총 지출 546,020원 · 67건"처럼 떴는데,
546,020원은 순수 지출(`kind:"expense"`)만 더한 값인 반면 67건은 그 화면에 같이 섞여
보이는 대리결제·입출금 항목까지 전부 합친 개수라 "이 67건을 더하면 이 금액"처럼
보이지만 실제로는 그 중 일부만 그 합계에 들어감 — 건수를 `combined.length`(전체 표시
줄 수)에서 `categoryExpenses.length`(실제로 그 금액에 들어간 지출 개수)로 맞춤.
`Ledger.jsx`의 totalsLine, "전체 흐름"(`category==="all"`) 분기만 해당 — "카드"/"대리결제"/
"입출금" 필터는 원래도 그 필터에 맞는 건수만 세고 있어서 문제없음. Playwright로 지출
2건+입금 1건을 섞어서 재현 후 "총 지출 20,001원 · 2건"으로 정확히 뜨는 것 확인.

### 2026-08-24(추가): "최신순"이 실제로는 등록 순서였던 것
실사용 중 발견 — "주택청약 자동이체"(08-15 날짜)가 최신순인데도 08-20~08-22 날짜
기록들보다 계속 위에 떠 있다는 리포트. 원인: `combined`의 "최신순" 정렬이 `date`
필드가 아니라 `createdTime(item)`(id에 박힌, **이 기록이 앱에 실제로 만들어진
시각**)으로 되어 있었음. 평소엔 사람이 그날그날 바로 기록하니 등록순≈날짜순이라
안 보이던 문제인데, 자동이체처럼 결제일과 앱에 반영되는 시각이 어긋나는 항목이나
지난 날짜로 나중에 수정/등록한 기록은 실제 날짜와 무관하게 "언제 등록됐는지"로만
줄이 매겨져서 순서가 뒤틀려 보였음. `date`(항상 "YYYY-MM-DD") 기준 내림차순으로
바꾸고, 같은 날짜끼리만 기존처럼 등록 시각(`createdTime`)으로 순서를 정하게 함.
Playwright로 "오늘 날짜 지출을 먼저 등록 → 그다음 과거 날짜 지출을 등록"해서 등록
순서와 날짜 순서를 일부러 어긋나게 재현 — 고치기 전이라면 나중에 등록한 과거 날짜
항목이 위로 왔을 상황인데, 고친 뒤엔 최근 날짜(오늘것)가 정상적으로 위에 뜨는 것
확인.

### 2026-08-24(추가): "라벨이 말하는 것과 실제 계산이 다른" 종류의 버그를 다시 훑음
위 최신순 버그와 같은 계열("이름은 그럴듯한데 실제 계산이 다름")을 사용자가 다시
지적하면서 "전체적으로 이런 거 또 있나 처음부터 다시 재검토"를 요청. 모든 화면의
요약/비교 숫자를 라벨과 대조하는 방식으로 다시 훑었고, 하나 더 찾음:

- **홈 "이번 달 요약"의 "지난달 대비 X%"가 지난달 "전체"와 이번 달 "지금까지(진행
  중)"를 그냥 비교하고 있었음** — 월초일수록 비교 기간 길이가 달라서 실제 소비
  속도와 무관하게 무조건 크게 줄어든 것처럼 뜨는 구조였음. 달력 탭의 "지난달
  이맘때보다" 배지는 이미 같은 문제를 날짜 컷오프로 올바르게 처리하고 있었는데,
  홈 화면에는 그 방식이 안 쓰이고 있었음. 거기다 지난달 합계(`prevTotalSpent`)
  자체도 "카드반영"으로 생긴 `isCardAdjustment` 항목을 실제 지출 합산과
  `fixedSumAll` 예측치 양쪽에서 이중으로 세고 있어서, 카드반영을 쓴 달일수록 지난달
  총액이 부풀려져 있었음(이번 달 계산엔 이미 있던 이중계산 방지 로직이 지난달
  계산엔 빠져 있었음).
  - `App.jsx`에 `prevTotalSpentToDate`를 새로 추가(지난달도 오늘과 같은 날짜까지만
    자름, 고정지출은 날짜 개념이 없어서 안 자름) + `prevTotalSpent` 자체의
    이중계산도 같이 고침(`prevNormalSpent`/`prevCardSpent`로 나눠서 계산, 이번 달과
    같은 방식).
  - `Home.jsx` `SummaryCard`가 `prevTotalSpentToDate`를 쓰도록 바꾸고, 라벨도
    "지난달 대비" → 실제로 컷오프해서 비교한다는 뜻이 드러나게 "지난달 이맘때보다"로
    (달력 탭과 같은 표현으로 통일).
  - 순수 로직 재현 스크립트로 이중계산 제거·날짜 컷오프 두 가지를 각각 검증(PASS)한
    뒤, 실제 브라우저로도 지난달 컷오프 밖 날짜(7/28)에 큰 지출을 넣고 컷오프 안
    날짜(7/5)엔 작은 지출을 넣어서 "지난달 이맘때보다 +50%"가 정확히(컷오프 밖
    지출은 제외한 채) 계산되는 것을 확인. 프로덕션 빌드 기준 전체 회귀 테스트도
    재통과.
- 그 외 화면들(달력 비교 배지, 홈의 "반영 전 여유", 월마감 요약, 카드/할부 상세
  등)의 라벨-계산 일치 여부도 같이 재검토했고, 추가로 발견된 문제는 없었음. 달력
  탭의 비교 배지가 고정지출을 아예 안 세는 것과 홈 화면 비교가 고정지출을 세는 것은
  서로 다른 버그가 아니라 각 화면 안에서 "총 지출" 자체의 정의가 원래 다르게
  일관되어 있어서(달력은 처음부터 날짜별 개별 기록만 다루는 화면, 홈은 고정지출까지
  포함한 개념) 문제 없다고 결론 내림.

### 2026-08-24(추가2): 3차 재검토 — "건수" 라벨을 전부 다시 대조
"할 때마다 나오는 거 보면 더 있을 거 같다"는 사용자 지적에, 이번엔 코드 전체에서
"건"이 붙는 라벨을 전부 grep으로 뽑아서 하나씩 금액과 대조하는 방식으로 다시 훑음.
- **내역 "카드" 필터의 합계 줄도 같은 병이었음** — `카드값에 반영된 지출 합계
  {cardListTotal} · {categoryExpenses.length}건`. `cardListTotal`은 카드 지출 +
  대리결제 정산에서 카드값에 얹힌 부족분까지 더한 값인데, 옆 건수는 카드 지출
  개수만 셌음("전체 흐름"에서 고친 것과 똑같은 종류). 부족분에 걸린 대리결제
  건수도 같이 세도록 `cardListCount` 추가(`Ledger.jsx`).
- **`removeAccount`(통장 삭제)가 `removeCard`와 달리 `fixedExpenses`를 안
  건드리고 있었음** — 카드 삭제 땐 이미 고쳐둔 건데(2026-08-23) 통장 쪽엔 같은
  걸 안 해뒀던 걸 발견. 통장형 고정지출의 `accountId`가 삭제된 통장을 계속
  가리키면 자동이체 대상 통장 표시가 비거나(다행히 실제 처리 함수들엔 이미
  방어 코드가 있어서 돈 계산 자체는 안전했음) 매번 조용히 첫 통장으로 대체되는
  게 반복됨 — 카드 때와 같은 패턴으로 삭제 시점에 남은 첫 통장으로 재배정하고
  확인창에 영향 건수를 안내하게 함.
- 그 외에 리듀서/합계 계산 전반(정산·조정·이체·JSON 내보내기·가져오기 왕복)을
  다시 훑었고 추가로 발견된 문제는 없었음.

검증: 레거시 대리결제 데이터를 직접 주입해서 카드 필터 합계 줄이 "12,500원 ·
2건"으로 정확히(부족분 대리결제까지 포함해서) 뜨는 것 확인. 통장 2개 중 하나에
고정지출을 연결해두고 그 통장을 삭제 → 확인창에 "고정지출 1건은 남은 통장으로
옮겨져요" 안내가 뜨고, 실제로 남은 통장 id로 재배정되는 것까지 확인. 콘솔 에러
없음.

### 2026-09-08: 정산받은 돈이 어느 합계에도 안 잡히던 것
실사용 중 발견 — 앞서 8/24까지 세 번 훑은 "라벨과 계산이 어긋난다" 계열의 **네 번째**
사례이고, 그중 금액이 가장 크게 틀어지던 것. 2026-08-21에 만든 `reimbursedAmount`
(대신 내주고 나중에 돌려받은 돈)를 **어느 합계도 읽고 있지 않았음.** 지출 줄에는
"정산받음 N원" 태그가 붙는데 홈 게이지·달력·내역 합계·월마감 요약은 전부 원래 결제
금액을 그대로 더하고 있어서, 남의 몫까지 내가 쓴 돈으로 세고 있었음. 대리결제가 잦은
달일수록 여유가 실제보다 적게 보임.

- `lib/data.js`에 **`netAmount(e) = amount − reimbursedAmount`**를 추가하고,
  **지출 합계는 전부 이걸로 내도록** 바꿈(`App.jsx`의 `normalSpent`·`cardSpentThisCycle`·
  `todaySpent`·지난달 합계, `Calendar.jsx`의 월합계·일별 히트맵·카테고리별·비교 배지,
  `Ledger.jsx`의 `totalSpent`·`cardListTotal`, `Gates.jsx`의 월마감 요약).
- **카드 지출만 예외라 따로 뺀다.** 카드로 대신 내준 건은 `spent`에 `cardBillTotal`
  (카드사에 낼 실제 청구액)로 들어가는데, 청구액은 돌려받은 것과 무관하게 전액
  그대로라 `netAmount`로는 안 빠짐. 그래서 `cardReimbursedThisCycle`을 따로 만들어
  `spent`에서 한 번 뺌. 현금으로 대신 낸 것은 `normalSpent`가 이미 net이라 여기서
  또 세면 두 번 빠짐 — 그래서 카드분만 셈.
- **줄마다 보이는 금액은 실제 결제액 그대로 둠.** 카드값 대조할 때 필요한 숫자라서.
  그러면 눈으로 더한 값과 합계가 안 맞는데, 그 차이가 곧 돌려받은 돈이므로 총계 옆에
  **"정산받은 N원은 뺐어요"**를 적어 둠(`Ledger.jsx`, `reimbursedInView`).

### 2026-09-08: 결제 문자를 공유로 넘기면 기록 화면이 채워진 채로 열림
안드로이드에서 문자·알림을 '공유 → 내돈챙겨줘'로 보내면 `manifest.webmanifest`의
`share_target`을 타고 `?text=...`로 열림. MacroDroid 같은 자동화 앱이 같은 주소를 직접
열어도 동일하게 동작(`?paste=...`도 받음). `App.jsx`가 그 값을 `pendingText`로 잡아
`ctx`에 실어 주고 `Add.jsx`가 열자마자 `parsePaymentText`로 금액·가맹점·날짜를 채움.
**주소는 곧바로 지움** — 안 지우면 새로고침할 때마다 같은 문자가 다시 올라오고, 홈
화면에 그 주소가 저장되면 앱을 켤 때마다 뜸.

이 경로는 **채우기만 하고 등록은 사람이 누름.** 아래 자동 등록과는 다른 길이다.

### 2026-09-08: 안드로이드 껍데기 + 결제 알림 자동 등록
"결제 문자가 오면 알아서 등록되게 해달라"는 요청. 웹앱은 알림을 읽을 방법이 아예 없어서
(권한 문제가 아니라 기능이 없음) 그 부분만 네이티브 껍데기로 뺐음 — 자세한 건 **§10**.
웹 쪽에 들어온 것은 다음 셋:

- **`lib/native.js`** — 껍데기와의 다리. `window.PassbookNative`가 없으면 전부 조용히
  넘어감(크롬에서 열어도 아무것도 안 깨지고 그냥 이 기능이 없는 것처럼 굶). 알림 큐
  가져오기(`pullPendingPayments` — **가져오면 저쪽 큐는 비워짐**), 권한 확인·설정 열기,
  백업 저장·목록·읽기.
- **`lib/auto-record.js`** — 알림을 확인 없이 기록으로 넣는 규칙. 아래 §4에 정리.
- **`components/payment-inbox.jsx`** — 자동으로 못 넣은 것을 기록 탭 맨 위에 보여주고,
  '이걸로 채우기'/'버리기'를 제공. 알림 권한이 꺼져 있으면 그 안내부터 띄움(껍데기가
  아닌 크롬에서는 아예 안 그림 — 있을 수 없는 기능이라 안내조차 소음).

설정 → **결제 알림** 섹션에 켜고 끄는 스위치가 있음(`data.autoRecord`, 기본 켜짐).
껍데기 안에서만 보임. 끄면 자동으로 안 넣고 전부 알림함에 모아만 둠.

자동 백업도 껍데기가 함 — `App.jsx`가 화면을 벗어날 때(`visibilitychange` hidden /
껍데기의 `passbook-native-pause`) `saveBackup(data)`을 한 번 부름. 데이터가 바뀔 때마다
저장하면 글자 하나 칠 때마다 파일을 쓰게 되고, 기록을 남기고 앱을 닫는 게 가장 흔한
흐름이라 그 순간이 데이터가 가장 최신인 지점이라서.

### 2026-09-09: 자동 등록 훑기 — 카드값이 두 배가 되던 것
9월 8일에 들어온 자동 등록을 처음부터 다시 대조하면서 셋을 고침. 앞의 것이 이 저장소의
고질(**라벨과 실제 계산이 어긋남**)과 같은 계열이라 특히 조용히 틀렸음.

- **카드 할부가 자동 등록에 걸리면 그 달 카드값이 정확히 두 배가 됐음.** `matchFixed`가
  카드 고정지출을 할부·매달반복 구분 없이 짝으로 봤는데, 할부는 이미 `fixedPortion`으로
  카드값에 들어가 있음. 짝이 지어지면 `bill`에 또 더해져서 `cardTotals.total =
  bill + fixedPortion`이 같은 금액을 두 번 셈. `paidMonths`를 적어도 소용없음
  (`unpaidFixed`가 할부는 안 봄). 재현: 50만 원짜리 할부 하나 있는 상태에서 50만 원
  승인 알림 → 카드값 50만 → **100만**. 고친 뒤 50만 유지, 알림함으로 넘어감.
- **문자 날짜가 미래로 밀리거나 달력에 없는 날이 될 수 있었음**(`parsePaymentText`).
  문자에 연도가 없어서 늘 올해로 읽었는데, 12/31 문자를 1월에 받으면 **1년 뒤**가 되어
  이번 달 합계에서 통째로 빠짐. 또 `(\d{1,2})[\/.\-](\d{1,2})`가 "19.99" 같은 것도
  잡아서 `2026-19-99` 같은 날짜가 만들어질 수 있었는데, 그러면 그 기록은 **어느 달
  목록에도 안 잡혀 조용히 사라짐**(삭제된 것도 아니라 찾을 방법도 없음). 달력에 실제로
  있는 날일 때만 쓰고, 앞선 날짜는 작년으로 봄(시차·지연 감안 일주일 여유).
- **처리 완료로 적는 달이 늘 '지금'이었음.** 지난달 결제 알림을 이번 달에 읽으면 지난달
  것은 미처리로 남고 이번 달 것이 처리된 것으로 표시됨 — 양쪽이 다 틀림. 그 기록이
  실제로 일어난 달로 적게 바꿈(`linkedFixedMonth`도 같이).

- **한 번 보류한 알림을 데이터가 바뀔 때마다 다시 판단하고 있었음**(`App.jsx`).
  자동 등록 효과가 `[inbox, data]`에 걸려 있어서, `data`가 바뀔 때마다 알림함 **전체**를
  다시 돌렸음. 그러면 "애매하니 사람이 보라"고 미뤄 둔 것이 나중에 엉뚱한 데이터
  변경에 편승해 조용히 들어감 — 실제로 브라우저 검증 중에 **백업을 되돌렸더니 알림함에
  있던 할부 건이 복원된 데이터에 딸려 들어가** 카드값이 0 → 500,000이 되는 걸 봤음.
  보류했다는 건 사람이 보고 정하라는 뜻이므로, 새로 들어온 것만 판단하고 판단이 끝난
  것에는 `checked` 표시를 남겨 다시 안 돌게 함. 그다음은 '채우기'로만 들어감.

검증: 순수 로직 재현 스크립트로 위 셋을 각각 확인하고, 깨지면 안 되는 것 넷(카드
매달반복 즉시 기록·`isCardAdjustment`·처리 완료 표시·금액 같은 통장 고정지출을 적요로
가르기)도 같이 통과시킴. 그다음 프로덕션 빌드를 `vite preview`로 띄우고 껍데기
다리(`window.PassbookNative`)를 흉내내 **실제 브라우저에서** 확인함 — 할부 승인은
알림함에 남고 카드값 500,000원 유지, 유튜브 승인은 바로 기록되며 카드값 514,900원·
여유와 총 지출은 그대로(이중계산 없음)·미처리 배지 2건→1건, 백업 되돌리기는 백업
내용 그대로 복원되고 보류된 알림은 딸려 들어오지 않음. 콘솔 에러 없음.

### 2026-09-09: 백업을 앱 안에서 되돌릴 수 있게
껍데기가 매일 백업을 남기는데(`Download/내돈챙겨줘/`, 30일치) **앱에서 그걸 읽는 화면이
없었음** — `native.js`에 `listBackups`/`readBackup`을 만들어 두고 아무 데서도 안 쓰고
있었음. 그래서 되돌리려면 파일 관리자로 파일을 찾아 열고 본문을 복사해 '가져오기'에
붙여넣어야 했는데, 정작 복구가 필요한 순간(기기를 바꿨거나 앱을 지웠다 깐 직후)에 그
과정을 해내기 어려움. **백업은 되돌릴 수 있어야 백업임.**

설정 → 데이터 백업에 **'휴대폰에 저장된 백업'**을 추가(껍데기 안에서만 보임). 날짜와
파일 크기를 최신순으로 보여주고 '되돌리기'를 누르면 그날 상태로 복원. 덮어쓰기는
되돌릴 수 없어서 '가져오기'와 똑같이 **확인창을 한 번 거침**(그 백업에 든 지출 건수도
같이 보여줌).

### 2026-09-11: 디자인 손보기 — 홈·기록·내역
설치한 디자인 스킬(`better-interface` 묶음: 접근성·레이아웃·문구·글꼴·색·UI)의 기준으로
매일 쓰는 세 탭을 휴대폰 크기(412px·320px)에서 흰색 테마로 진단하고, 근거가 있는
것만 고쳤다. 취향으로 갈아엎은 건 없다 — 전부 잰 값이나 실제 동작에서 나온 것.

- **주 버튼 글자가 흐릿했다.** 금색 바탕 위 글자를 모든 테마에 `#23190C`로 박아 둬서,
  금색이 어두운 밝은 테마 5개에서 대비가 3.2~3.9(기준 4.5). 흰색 테마의 '기록하기'가
  회색 위 검은 글자였다. `onGold` 토큰으로 테마마다 따로 맞춤. 베이지·청록은 흰 글자로도
  모자라 금색을 아주 조금 진하게(#9C6F28→#845E22, #2E7D6E→#2B7567).
- **홈 배너가 가리키는 곳에 아무것도 없었다.** "아래에서 출금처리 하세요"인데 그 버튼들은
  맨 아래 작은 '펼치기'를 눌러야 나오는 접힌 영역 안에 있었고, 배너는 눌러도 아무 일이
  없었다. 이제 배너를 누르면 예산 관리가 펼쳐지며 그 자리로 간다.
- **'완료'를 누르면 확인 없이 기록이 지워졌다.** 상태 표시처럼 보이는 '완료'가 사실은
  되돌리기 버튼이라, 스치기만 해도 딸린 출금 기록(자동으로 들어온 것 포함)이 사라졌다.
  이제 무엇이 지워지는지 확인창으로 묻는다. 버튼도 높이 20px 남짓·글자 11px에서 32px로.
- **입출금 줄에 '자동' 배지가 없었다.** 은행 알림으로 자동 등록된 입출금은 `auto`가
  있는데 줄이 안 그려서, 자동 등록 규칙("넣은 것엔 '자동' 배지")이 은행 쪽에선 안
  지켜지고 있었다. 어느 통장인지도 같이 적는다.
- **내역 줄 세 종류의 틀이 달랐다.** 지출은 날짜가 아래 줄 `2026-09-10`, 입출금은 금액
  옆 `09-10`이고 메모가 제목에 붙어 세 줄로 접혔다. 셋 다 [제목·배지 / 메모 / 날짜]로.
  흐린 글자 색이 코드에 박혀 있어 테마를 안 따라갔고 전 테마에서 대비 미달 → `inkMuted`.
- **입력칸 경계가 거의 안 보였고, 어느 칸에 들어가 있는지도 안 보였다.** 테두리 대비
  1.25~1.49 → `field`로 3:1. `outline:none`만 있고 대신할 표시가 없던 것 → 전역
  `:focus-visible`.
- **아이콘만 있는 버튼에 이름이 없었다**(수정·삭제·검색 등, `aria-label`이 코드 전체에
  0개). 휴지통은 누르는 영역도 22px 남짓이라 금액 옆에서 잘못 누르기 쉬웠다 → 40px.
- **라벨이 칸과 안 이어져 있었다**(`<label>`이 코드 전체에 0개). 기록 화면의 금액·
  카테고리·카드·통장·날짜·메모를 이었다 — 라벨을 눌러도 칸에 들어간다.
- **320px 화면에서 내역의 검색 버튼이 화면 밖으로 잘렸다.** '전체 흐름' 칸이 줄어들게 함.
- 그 밖에: '할부(고정지출)'이 '고정지/출'로 잘리던 것(한글 단어 단위 줄바꿈),
  게이지 바탕 링이 모든 밝은 테마에 베이지로 박혀 있던 것, 움직임 줄이기 설정 무시,
  누를 때 살짝 눌리는 느낌(0.96) 추가, 금액 숫자 폭 고정(tabular-nums).

검증: Playwright(임시 폴더에 설치, 프로젝트엔 안 넣음)로 실사용과 비슷한 데이터를 넣고
흰색·검정 테마 전후를 찍어 비교. 배너→처리 목록 이동, '완료' 확인창과 취소 시 데이터
그대로, 라벨→칸 포커스 4곳, 포커스 테두리, 버튼 글자색, 이름 없는 버튼 0개(세 탭),
320px에서 옆으로 넘침 없음을 스크립트로 확인. 콘솔 오류 없음.

**안 한 것** — 이번 범위(홈·기록·내역, 흰색 테마) 밖이라 남겨 둠:
- 검정 테마에서 종이 카드 위 금색 글자(카드 이름 배지·연필 아이콘)가 크림 바탕에서
  대비 약 2:1. 검정 테마는 바탕이 어둡고 종이만 밝아서, 금색·초록·빨강이 종이 위에서
  따로 필요하다. 토큰을 더 나눠야 하는 일이라 따로 할 것.
- 글자 크기가 11~21.5px 사이에 14가지쯤 흩어져 있다. 크기 체계를 정하는 건 화면
  전체를 건드리는 일이라 이번엔 안 함.
- 달력·설정·온보딩은 진단하지 않았다.

### 배포
GitHub 저장소 최상단에 폴더 안 파일들을 올리고 → Vercel "Add New Project" → Vite 자동 인식 → Deploy.
**재배포는 반드시 같은 프로젝트/같은 도메인으로.** localStorage는 도메인에 묶여 있어서 주소가 바뀌면 기존 데이터가 전부 날아감.

---

## 2. 가장 중요한 규칙 (반드시 지킬 것)

### STORAGE_KEY 고정
```js
const STORAGE_KEY = "passbook-data-v4";
```
**절대 바꾸지 말 것.** 바꾸면 실사용자 데이터가 전부 사라짐.

### migrate() 유지
`migrate(raw)` 함수가 구버전 데이터 구조를 자동 변환함 (`light` 테마 → `beige`, `cardBill` → `cards[]`, `account` → `accounts[]`, `salary` → `spendingGoal` 등).
데이터 구조를 바꿀 때는 **반드시 migrate에 변환 로직을 추가**해야 기존 사용자가 깨지지 않음.

### 실사용 중
개발자 본인이 실제로 매일 쓰고 있고 지인 공유 예정. 데이터 손실 위험이 있는 변경은 신중히.

---

## 3. 데이터 모델 (defaultData)

```js
{
  theme: "dark",              // THEMES 키 (6종)
  onboarded: false,           // 온보딩 완료 여부
  lastSeenMonth: null,        // 월 마감 요약 표시 기준 (예: "2026-07")
  spendingGoal: 0,            // 이번 달 목표 지출액
  accounts: [{ id, name, initialBalance }],        // 통장(다중)
  cards:    [{ id, name, bill }],                  // 카드(다중). bill = 일시불 누적분
  categories: [{ id, name, color }],
  expenses: [...],
  fixedExpenses: [...],
  balanceEntries: [...],
}
```
`autoRecord`(결제 알림 자동 등록 on/off)는 **`defaultData()`에 없고 `migrate()`가
채움** — `raw.autoRecord !== false`라 옛 백업으로 들어와도 기본 켜짐. 껍데기 앱
안에서만 설정 화면에 스위치가 보임.

`pinLock`·`trash`·카테고리 `budget` 필드는 2026-08-21에 제거됨. `migrate()`가 옛 백업에 남아있어도 조용히 지움 — 다시 쓰지 말 것.

### expenses[] (지출)
```js
{
  id, amount, categoryId, date, memo,
  paymentMethod: "card" | "cash",
  cardId,                    // 카드 결제 시
  linkedBalanceId,           // 현금(통장) 결제 시 생성된 balanceEntry id
  isReceivable,              // 대리결제(추후 정산) 여부 — 2026-08-21부터 새로 안 만듦(레거시 전용, 아래 참고)
  settled, repaidAmount, settledAt,
  settlementCardDelta, settlementCardId, settlementBalanceId,  // 정산 시 되돌리기용 (레거시)
  linkedTransitMonth,        // 대중교통 누적 항목 표시 (예: "2026-07")
  isCardAdjustment,          // 카드값에 흔적 남기기용(2026-08-21). "카드반영"/카드값 수동
                              // 추가로 생긴 항목 표시. cardSpentThisCycle 집계에서는 제외
                              // (fixedSumAll이 이미 매달 반영하고 있어서 이중계산 방지)
  auto,                      // 2026-09-08: 알림에서 확인 없이 자동으로 들어온 줄.
                              // 내역에서 '자동' 배지로 보임 — 이 표시가 없으면 자동
                              // 등록은 위험한 기능이 됨(이상해도 어느 줄인지 못 찾음)
  autoTime,                  // 2026-09-11: 문자에 찍힌 결제 시각 "17:15". 같은 날·같은
                              // 금액의 두 알림이 같은 거래인지 가르는 데 씀. 선택 칸이라
                              // 없어도 되고 migrate()는 안 건드림
  reimbursedAmount, reimbursedAt, reimbursementBalanceId,  // 2026-08-21 신규: 대리결제를
                              // isReceivable 없이 평범한 지출로 기록하고, 나중에 내역에서
                              // "정산받음"으로 표시할 때 씀. 원래 지출 금액은 안 건드리고
                              // 받은 돈만 balanceEntries에 순수 입금(type:"in")으로 남김
}
```

### fixedExpenses[] (고정지출/할부)
```js
{
  id, name, baseAmount,
  totalMonths,               // 0이면 매달 반복, 1 이상이면 할부
  startInstallment,          // 등록 시점의 회차
  setupMonthKey,             // 등록한 달
  overrides: { "2026-07": 55000 },   // 특정 달만 금액 다르게
  paymentMethod: "card" | "cash",
  cardId, accountId,
  paidMonths: { "2026-07": entryId | true },  // 출금/카드반영 처리 여부
  autoPayDay,                // 통장형 자동 출금처리 날짜 (1~31, null이면 수동)
  auto, purchaseAmount, autoTime,  // 2026-09-11: 할부 결제 알림으로 자동 등록한 할부.
                              // purchaseAmount = 결제 총액(취소 알림과 짝 맞추기용). 선택 칸
}
```

### balanceEntries[] (통장 입출금)
```js
{
  id, accountId, type: "in" | "out", amount, date, memo,
  linkedFixedId, linkedFixedMonth,   // 고정지출 출금처리로 생성된 경우
  isAdjustment,                       // 잔액 스냅으로 생성된 조정 기록
  transferId,                         // 통장 간 이체 (같은 id를 가진 out/in 한 쌍)
  auto,                               // 2026-09-08: 은행 알림에서 자동으로 들어온 줄
  autoTime,                           // 2026-09-11: 문자에 찍힌 거래 시각 (지출과 같음)
}
```

---

## 4. 핵심 도메인 로직 (⚠️ 여기가 제일 헷갈리는 부분)

### 고정지출 3분류 — 자동 반영 여부가 다름

| 분류 | 조건 | 카드값/예산 반영 |
|---|---|---|
| 통장형 | `paymentMethod === "cash"` | 예산엔 자동 포함. 통장 출금은 **수동 "출금처리"** 또는 `autoPayDay` 자동 |
| 카드 할부 | `paymentMethod === "card"` && `totalMonths > 0` | **자동으로 카드값에 포함** (`cardTotals.fixedPortion`) |
| 카드 매달반복 | `paymentMethod === "card"` && `totalMonths === 0` | 자동 반영 **안 함**. 홈에서 **"카드반영"** 버튼 눌러야 카드값에 더해짐 |

> 카드 매달반복(휴대폰요금·유튜브·쿠팡멤버십 등)을 자동 반영하지 않는 이유: 결제일이 제각각이라 카드 앱 기준으로 실시간 대조할 때 오차가 생겼기 때문.

### 카드 금액 2층 구조
```js
cardTotals = cards.map(c => ({
  ...c,
  fixedPortion,                  // 이번 달 할부분 (자동 계산, 저장 안 됨)
  total: c.bill + fixedPortion,  // 화면에 보이는 금액
}))
```
- `c.bill` = 일시불/직접등록분 **누적 저장값**
- `fixedPortion` = 매달 다시 계산되는 값, 저장하지 않음
- **`reconcileCard`(맞추기)**: 사용자는 카드 앱의 **할부 포함 총액**을 입력 → 내부에서 `fixedPortion`을 빼고 `bill`에 저장. (이거 안 하면 이중계산 버그)
- **`payCard`(결제하기)**: 통장에서 `bill + fixedPortion` **전액** 출금, `bill`만 0으로 리셋. 할부는 다음 달 자동으로 다시 계산됨.

### 예산 계산 2종
```js
spent          = normalSpent + fixedSum + cardBillTotal - cardReimbursedThisCycle
remaining      = spendingGoal - spent            // ★ 홈 원형게이지 메인값
processedSpent = spent - unpaidFixedSum
realRemaining  = spendingGoal - processedSpent   // 서브 "반영 전 여유"
```
`normalSpent`를 비롯한 **모든 지출 합계는 `netAmount`로 냄**(바로 아래 참고).
`cardReimbursedThisCycle`만 따로 빼는 이유도 거기 적어 둠.
- **메인 = `remaining`**: 아직 출금처리 안 했어도 전부 미리 반영한 "이번 달 운용 가능"
- **서브 = `realRemaining`**: 미처리분을 뺀 값. UI 표기는 **"반영 전 여유"** (예전 "실제 여유"는 오해 소지가 있어 변경됨)

### netAmount — 지출 합계는 전부 이걸로 (2026-09-08)
```js
netAmount(e) = e.amount - (e.reimbursedAmount || 0)   // lib/data.js
```
대신 내주고 돌려받은 돈은 **내가 쓴 돈이 아님.** 그래서 어떤 화면에서든 지출을 더할
때는 `e.amount`가 아니라 `netAmount(e)`를 씀. 이걸 안 지키면 대리결제가 잦은 달에
여유가 실제보다 적게 보이고, 그게 2026-09-08에 실제로 있었던 버그임.

**단 카드 지출은 이 규칙만으로 안 끝남.** 카드로 대신 내준 건은 예산에
`cardBillTotal`(카드사에 낼 **청구액**)로 들어가는데, 청구액은 돌려받은 것과 무관하게
전액 그대로라 `netAmount`로는 안 빠짐. 그래서 `cardReimbursedThisCycle`(이번 달 카드
지출의 `reimbursedAmount` 합)을 `spent`에서 한 번 더 뺌. **현금 지출은 여기 세면 안
됨** — `normalSpent`가 이미 net이라 두 번 빠짐.

**줄마다 보이는 금액은 실제 결제액 그대로 둠**(카드 앱과 대조할 때 필요한 숫자라서).
그래서 눈으로 더한 값과 합계가 다르고, 그 차이가 곧 돌려받은 돈임 — 내역 총계 옆의
"정산받은 N원은 뺐어요"가 그 설명임.

### 자동 등록 규칙 (`lib/auto-record.js`, 2026-09-08)
껍데기가 넘긴 알림을 사람 확인 없이 기록으로 넣는 자리. **애매하면 넣지 않고 알림함에
남김 — 놓친 건 나중에 넣으면 되지만 잘못 들어간 건 찾기 어렵기 때문.**

| 알림 | 어디로 | 여유에 |
|---|---|---|
| 카드 승인 | 지출 기록 + 그 카드 `bill` 증가 | 들어감 |
| 은행 입출금 | 통장 입출금(`balanceEntries`) | 안 들어감(잔고만) |

- **카드를 먼저 본다.** 카드 승인 문구에도 '결제'가 들어가서, 은행부터 보면 카드 결제가
  통장 출금으로 새어 들어감.
- **카드로 보는 조건**: `승인|결제`가 있고 `취소|환불|거절|실패|누적|한도`가 없을 것.
- **은행으로 보는 조건**: 위 부정어가 없고 `입금|이체입금|받으심`(들어옴) 또는
  `출금|이체출금|자동이체|납부`(나감) 중 하나가 있을 것.
- **어느 카드·통장인지 못 고르면 안 넣음.** 하나뿐이면 그것으로 보고, 아니면
  **보낸 앱(패키지)으로 먼저 가림**(2026-09-09). 은행 이름이 문구에 아예 없는 형식이
  흔하기 때문 — KB스타뱅킹 출금 알림에는 '국민'이 한 글자도 없고 대신 '카카오페이'
  같은 남의 이름이 들어 있어서, 글자만 보면 엉뚱한 통장에 붙음. `ISSUERS` 표에
  국민·신한·우리·하나·농협·카카오뱅크·카카오페이·토스·기업·현대·롯데·삼성·비씨·씨티.
- **보낸 곳을 아는데 그 이름의 통장·카드가 없으면 글자로 안 찍음.** 보낸 앱이 KB인 걸
  아는 이상 '카카오페이'는 확실히 틀린 답이라, 보류하고 사람에게 넘김. 보낸 곳을
  모를 때만 예전처럼 이름 앞 두 글자를 문구에서 찾음. 같은 은행 통장이 둘이면
  (주거래·비상금) 고르지 않음.
- **중복 방지 두 층**
  1. 같은 날·같은 금액·같은 카드인 지출이 이미 있으면 안 넣음. 입출금은 같은 날·같은
     금액·같은 방향·같은 통장이면 안 넣음(앱이 스스로 만드는 출금 — 카드값 결제,
     고정지출 출금처리, 대리결제 정산 — 과 겹치는 걸 막는 자리).
  2. **고정지출은 날짜로 못 막음.** 정기결제·자동이체는 통장에서 먼저 빠지고 사람은
     며칠 뒤에 '카드반영'/'출금처리'를 누름. 그래서 날짜 창을 넓히는 대신 **고정지출
     자체를 처리 완료로 표시**함(`paidMonths[curKey]`) — 버튼이 사라지므로 며칠이
     지나든 중복이 생길 수 없음. 날짜 창을 넓히면 같은 금액을 진짜로 두 번 쓴 경우
     (교통비 등)를 잘못 막게 됨.
- 짝을 맞추는 조건은 **금액 + 결제수단**이 둘 다 같을 것, 카드·통장이 지정돼 있으면
  그것도 같을 것. 금액만 보면 우연히 같은 금액의 다른 지출이 고정지출을 처리 완료로
  만들어 버림.
- **금액이 같은 고정지출이 여럿이면**(실제로 10만 원짜리가 데이트(여행)·가족모임·
  주택청약 셋) 알림 문구에 항목 이름의 조각이 있는 쪽을 고름(이름을 길게부터 짧게 훑음).
  **힌트가 없으면 아무것도 고르지 않고 알림함에 남김** — 잔액은 맞아도 이름이 뒤바뀌면
  그 달 내역이 조용히 거짓말을 하고, 되돌리려면 어느 것이 틀렸는지부터 찾아야 함.
- **카드 쪽 짝은 '매달반복'만.** 카드 할부(`totalMonths > 0`)에는 '카드반영' 버튼이
  없음 — 앱이 매달 `fixedPortion`으로 카드값에 이미 넣고 있어서, 짝을 지어 `bill`에
  또 더하면 **그 달 카드값이 정확히 두 배**가 됨(2026-09-09 수정). 통장 쪽은 할부여도
  괜찮음 — `fixedSum`이 처리 여부와 무관하게 항상 잡고 있어서 이중계산이 안 생김.
- **할부 회차와 금액이 같은 카드 승인은 아예 자동으로 안 넣음**(알림함으로). 문자만
  보고는 진짜 할부 청구인지 우연히 금액이 같은 다른 결제인지 가를 수 없어서.
- **처리 완료로 적는 달은 '지금'이 아니라 그 결제가 일어난 달**(2026-09-09 수정).
  12/31 결제 알림을 1/1에 읽으면 12월 것은 미처리로 남고 1월 것은 나가지도 않았는데
  처리된 것이 되어 양쪽이 다 틀림.
- **결제 뒤 취소**(2026-09-11): 같은 카드·같은 금액의 결제와 취소가 둘 다 기록 전이면
  (새로 온 것 + 알림함에 보류된 것) 서로 지운다. 결제가 이미 자동으로 들어갔으면 그
  기록을 빼고 카드값을 되돌린다 — `auto`인 것, 이번 달, `reimbursedAmount` 없는 것만.
  지난달 것은 안 건드리고 알림함에 '결제 취소'로 남김(채우기 없음). 예전엔 취소도 결제와
  똑같은 모양으로 알림함에 떠서 결제가 두 번 잡힌 것처럼 보였다.
  → 같은 날 "확인 안 누르고 자동으로" 요청으로, **손으로 적은 결제의 취소도 되돌린다**
  (자동 기록을 먼저 고르고, 없으면 손으로 적은 것. 가맹점 이름이 같은 걸 우선).
- **같은 거래인지는 결제 시각으로**(2026-09-11). 예전엔 같은 날·같은 금액이면 무조건
  보류해서 지하철을 하루 두 번 타면 두 번째가 알림함에 걸렸다. 그냥 풀면 같은 결제가
  앱 알림과 문자로 두 번 올 때 두 번 들어간다. 그래서 자동 기록에 문자의 시각을
  `autoTime`으로 남기고, 시각이 다르면 다른 거래·같으면 같은 거래로 본다. 손으로 적은
  것·앱이 만든 출금(카드값 결제·출금처리)은 시각이 없으니 같은 날·같은 금액이면 같은
  거래로 보고 **넘긴다**(보류 아님). '카드반영'·'출금처리'를 먼저 눌러 둔 고정지출은
  이름 조각이 문구에 있으면 같은 거래로 보고 넘긴다(예전엔 새 지출로 한 번 더 들어갔다).
- **이미 받은 알림은 다시 안 받는다**(`SEEN_KEY`, 최근 500건). 껍데기는 재시작·업데이트
  때 알림창에 남은 알림을 다시 넘기는데, 이미 되돌린 취소 알림이 짝을 못 찾아 알림함에
  괜히 뜨는 식의 일이 생긴다.
- **'누적'이 결제를 막고 있었다**(2026-09-11). 현대카드 문자는 늘 끝에 `누적○○원`이
  붙는데, '누적 사용액 안내'를 거르려고 '누적'을 부정어에 넣어 둬서 현대카드 결제가
  한 번도 자동으로 못 들어갔다. 카드 쪽 부정어에서 빼고, 대신 '결제 예정·청구·안내'를
  넣었다('결제'라는 글자 때문에 청구 안내가 지출로 들어갈 수 있었다).
- **할부 결제는 '할부(고정지출)'로 자동 등록**(2026-09-11). 문자에 총액이 찍혀서, 지출로
  넣으면 몇 달에 나눠 낼 돈이 이번 달 카드값에 통째로 잡힌다. 사람이 기록 탭에서 하던
  등록을 대신한다 — 이름 `가맹점 할부`, 월 금액 = 총액 ÷ 개월(나머지 원 단위는 첫 달
  `overrides`에), 첫 회차 = 결제한 달, `purchaseAmount`에 총액(취소 짝 맞추기용).
  개월 수를 못 읽으면('무이자할부'만 있고 '3개월'이 없으면) 나눌 수 없어 보류.
- 카드 정기결제로 넣을 땐 **`isCardAdjustment`를 붙임.** 안 붙이면 `fixedSumAll`이 이미
  매달 잡고 있는 금액과 겹쳐 예산에서 두 번 세어짐 — '카드반영' 버튼이 하는 일과 정확히
  같게 맞춘 것.
- **카테고리는 비워 둠**(`categoryId: null`). 문자에 그 정보가 없고 가맹점 이름으로
  짐작해 넣으면 틀린 카테고리가 조용히 쌓임. 미분류로 두면 내역에서 눈에 띔.
- 넣은 줄에는 **`auto: true`** — 내역에서 '자동' 배지.
- 반대 순서도 막음: 카드값을 은행 앱에서 먼저 내고 나중에 홈에서 '결제하기'를 누르면,
  같은 날 같은 금액의 출금이 있는지 보고 있으면 출금을 또 만들지 않음
  (`payCard`, Home.jsx) — "통장 출금은 이미 적혀 있어요"로 알려 줌.

### fixedSum vs fixedSumAll
- `fixedSum` = 통장형 + **아직 카드반영 안 한** 카드매달반복 (예산 계산용. 카드할부는 `cardBillTotal`에 이미 포함되므로, 카드매달반복은 반영되는 순간 `cardBillTotal`로 넘어가므로 각각 **중복 방지**)
  - 2026-08-21 수정: 카드반영을 누른 뒤에도 `fixedSum`이 계속 그 항목을 잡고 있어서 `spent`가 이중계산되던 버그를 고침. `fixedCardRecurringUnpaid`(=`paidMonths[curKey]` 없는 것만) 기준으로 바뀜.
- `fixedSumAll` = 전부, 반영 여부 무관 (화면 표시용 — 여긴 원래도 이중계산 없었음)

### ctx 객체
`App()`에서 모든 파생값을 계산해 `ctx` 하나로 만들어 자식 컴포넌트에 통째로 내려줌. 상태관리 라이브러리 없음.

---

## 5. 화면 구성

하단 탭 5개: **홈 / 기록 / 내역 / 달력 / 설정**

### 홈 (`HomeView`)
- 통장 잔액 카드(초록 테두리 = "실제 돈") — 다중 계좌면 탭해서 펼침, 입금/출금/**이체**, 실제 잔액 스냅
- 미처리 고정지출 배지
- 구분선 + 여백으로 시각 분리
- 원형 게이지(골드 = "계획") — 메인 `remaining`, 서브 "미처리 N원 · 반영 전 여유 N원"
- 월 요약 → 지출 현황(고정지출/현금지출) → 카드별 블록(맞추기·결제하기) → 대중교통 빠른입력 → **예산 관리(접기/펼치기)**
- 최근 기록 섹션은 **삭제됨** (내역과 중복이라)

### 기록 (`AddView`)
결제수단 3종: **카드 / 현금(통장) / 할부(고정지출)**
- 할부 선택 시 `InstallmentForm` 표시 (등록·수정·정렬)
- 금액은 `MoneyInput` (실시간 천단위 콤마 + "원")
- **결제 문자 붙여넣기 파싱** 버튼
- 2026-09-08: 맨 위에 **알림함**(`PaymentInbox`) — 껍데기가 잡았는데 자동으로 못 넣은
  알림을 보여주고 '이걸로 채우기'/'버리기'. 알림 권한이 꺼져 있으면 그 안내부터 띄움.
  크롬에서는 아예 안 그림. 공유(`share_target`)로 넘어온 문자도 열자마자 폼에 채워짐 —
  **둘 다 채우기만 하고 등록은 사람이 누름**(자동 등록은 이 길이 아니라 `auto-record.js`)
- 대리결제 체크박스 **없음** (2026-08-21에 제거) — 대신 내준 돈도 그냥 평범하게 카드/현금
  지출로 기록. 나중에 돌려받으면 내역에서 "정산받음"으로 표시 (아래 `LedgerView` 참고)
- 2026-08-22: 카테고리·카드·통장 선택을 칩 버튼 → **셀렉트**로 컴팩트화(같은 줄에 카테고리+
  카드/통장). "+ 새 카테고리"는 셀렉트 맨 아래 옵션으로 흡수. `InstallmentForm`의 카드/통장
  선택도 동일. 빠른금액 버튼(`QuickAmountButtons`)은 그대로 둠 — 새로 기록할 땐 실제로
  빠르게 누르는 용도라 컴팩트화 대상에서 제외(수정 폼과는 다른 판단)

### 내역 (`LedgerView`) — 2026-08-21에 시간범위×카테고리 2축 구조로 재설계
- **시간범위** (`timeScope`): 이번달 / 전체 / 기간 — 항상 상단 탭 3개
- **카테고리** (`category`): 전체 흐름(기본값) / 카드 / 대리결제 / 입출금 — "더보기" 드롭다운
- 두 축이 독립이라 "이번달만 대리결제" "전체 기간 입출금"처럼 아무 조합이나 가능
- `category === "all"`이면 지출·대리결제·입출금 **셋을 한 목록에 시간순으로 섞어서** 보여줌
  (`combined` 배열, `kind: "expense" | "receivable" | "balance"`로 구분해 각각
  `LedgerRow`/`ReceivableRow`/`BalanceRow`로 렌더링)
- 대리결제는 어느 카테고리에서 보든 정산상태 태그가 항상 붙음: 미정산 / 정산완료 /
  부족분 N원 카드값 / 초과분 N원 통장
- `category === "card"`일 때의 합계(`cardListTotal`)는 대리결제 원금 전체가 아니라
  **정산 부족분만** 더함 — 실제 `card.bill` 구성과 일치시키기 위함. 카드로 결제한
  대리결제 자체는 목록에 그대로 보이되(원금 표시), 합계엔 부족분만 반영
- 정렬: **최신순(날짜 기준, 같은 날짜끼리만 등록 시각)/높은금액순/낮은금액순** — 날짜
  그룹핑 없이 평면 리스트 (2026-08-24에 등록 시각 기준에서 고침)
- 2026-09-08: 합계는 전부 `netAmount`. 줄마다 보이는 금액은 실제 결제액이라 눈으로 더한
  값과 다른데, 그 차이를 총계 옆 **"정산받은 N원은 뺐어요"**로 설명함. 자동으로 들어온
  줄에는 **'자동' 배지**
- 지출 수정(연필) — 수정 시 카드값/통장 즉시 반영
- 대리결제(레거시, `isReceivable`)는 **미정산일 때만** 연필 버튼으로 그 자리에서 바로 정산(`settleReceivable`, Home.jsx에서 import) — 정산 끝나면 연필 사라지고 상태 태그로 바뀜. 정산완료 후엔 수정 UI 없음(다시 고치려면 삭제 후 재등록)
- 입출금은 삭제만 가능(수정 UI 없음)
- **일반 지출(카드/현금)의 정산받음**: `renderEditForm` 안에 "정산" 섹션이 따로 있음.
  `reimbursedAmount`가 없으면 "미리정산하기" 버튼 하나만 보이다가(`reimburseOpen` 상태로
  접힘/펼침) 누르면 입력폼(받은 금액 → `confirmReimburse`), 있으면 "정산받음 N원 · 취소"
  (`cancelReimburse`)로 바뀜. 원래 지출 금액·카드값/통장 차감은 안 건드리고, 받은 돈만
  `balanceEntries`에 순수 입금으로 추가 — 부족분/초과분 나눌 필요 없음(레거시
  `settleReceivable`과 다른 점). 목록 줄에는 "정산받음 N원" 태그로 표시(`LedgerRow`).
  삭제 시 연결된 입금 기록도 같이 삭제됨(`remove()`에서 `reimbursementBalanceId` 처리)
- 2026-08-22: 수정 폼(`renderEditForm`) 컴팩트하게 정리 — 카테고리/카드/통장 칩버튼 →
  셀렉트, 빠른금액 버튼 제거, 날짜+메모 한 줄. 삭제는 풀버튼 대신 휴지통 아이콘만
- 삭제는 **되돌릴 수 없음** — `window.confirm()` 한 번만 물어보고 바로 지움 (2026-08-21에 21일 휴지통 보관 기능을 걷어냄)

### 달력 (`CalendarView`)
일별 지출 합계, 히트맵(빨강 농도), 입금일 초록점, 최고지출일 금테두리, 지난달 대비 배지, 날짜 클릭 시 그날 내역

### 설정 (`SettingsView`)
섹션: 화면(테마 6종 3×2) / 예산 / 계좌·카드 / 카테고리(목록·삭제만) / **결제 알림** /
데이터(JSON 백업·복원)

데이터 백업의 **'휴대폰에 저장된 백업'**과 '결제 알림' 섹션은
**껍데기 앱 안에서만 보임**(`inNativeApp()`) — 크롬에는 알림을 읽을
방법이 없어서 스위치가 있어 봐야 눌러도 아무 일이 안 일어남. 켜면 카드 승인을 확인 없이
바로 기록하고, 끄면 알림함에 모아만 둠(`data.autoRecord`).

### 게이트 화면 (순서대로)
1. `Onboarding` — 8단계, 테마 실시간 미리보기 (`!data.onboarded`)
2. `MonthWrapUp` — 달이 바뀌면 지난달 요약 후 넘어가기

---

## 6. 테마 시스템

`THEMES` 객체에 6종(검정·베이지·흰색·청록·파랑·초록, 2026-08-21에 15종에서 추림). 각 테마는 동일한 키 세트를 가짐:
```js
{ id, label, swatch, mode, bg, bg2, navBg, paper, paperLine,
  ink, cream, gold, goldSoft, good, warn, danger, muted, border,
  onGold, field, inkMuted }        // 2026-09-11 추가
```
`THEME_ORDER`로 표시 순서 관리. `dark`만 `mode: "dark"`, 나머지는 전부 light 계열.
`useTheme()` 훅으로 어디서든 `T` 접근. **하드코딩 색상 쓰지 말고 반드시 `T.xxx` 사용.**

2026-09-11에 더한 세 토큰은 전부 **대비를 재서 정한 값**이다. 새 테마를 만들면 이 셋도
같은 기준으로 재서 넣을 것.

| 토큰 | 쓰는 곳 | 기준 |
|---|---|---|
| `onGold` | 금색 바탕 위 글자(주 버튼·선택된 탭·토스트) | 4.5:1 |
| `field` | 입력칸 테두리 | 입력칸·페이지 둘 다 3:1 |
| `inkMuted` | 종이(paper) 위 흐린 글자(내역 메모·날짜·배지) | 4.5:1 |

`primaryBtn(T)`·`inputSty(T)`가 이미 이걸 쓰므로 새 버튼·입력칸은 이 둘을 쓰면 된다.
금색 바탕에 `#23190C`를 직접 적지 말 것 — 그게 밝은 테마 5개에서 버튼 글자가 흐릿했던
원인이다.

테마 색 일부는 CSS 변수로도 나간다(`applyThemeVars`, `--focus`·`--field`·`--bg`).
인라인 style로는 `:focus-visible`·`:active`·`prefers-reduced-motion`을 못 쓰기 때문에
그 규칙들은 `index.html`에 있고 색만 변수로 받는다.

의미 구분: **초록(`T.good`) = 실제 돈 / 골드(`T.gold`) = 계획·목표**

---

## 7. 완료된 기능 요약

- 다중 통장·다중 카드, 통장 간 이체(양쪽 자동 기록, 삭제 시 쌍으로 삭제)
- 고정지출/할부 (할부 회차 자동 진행, 월별 금액 오버라이드, 같은 유형 내 카드↔카드·통장↔통장 재지정)
- 대신 내준 돈 정산받기 — **2026-08-21부터 새 방식**: 그냥 평범한 지출로 기록하고, 내역에서
  그 지출 수정 → "정산받음"에 받은 금액 입력 → 순수 입금으로 기록(`confirmReimburse`).
  구버전 `isReceivable` 방식(대리결제 체크 → 홈/내역에서 부족분→카드값·초과분→통장 자동
  반영, `settleReceivable`)은 레거시 데이터 호환용으로 남아있고 계속 동작함
- 잔액·카드값 스냅(맞추기), 고정지출 자동 출금처리(`autoPayDay`), 미처리 배지
- 대중교통비 빠른입력 — **누적 금액 갱신 방식**(더하지 않고 교체)
- 결제 문자 파싱(`parsePaymentText`) — 현대카드로 검증됨. "누적" 금액·마스킹 이름(`*`) 제외, 날짜 이후 단어를 가맹점명 우선
- JSON 백업/복원, 온보딩, 월 마감 요약
- 렌더링 중 에러가 나도 흰 화면 대신 안내+새로고침을 보여주는 `ErrorBoundary`
- **결제 알림 자동 등록**(2026-09-08) — 안드로이드 껍데기가 카드·은행 알림을 읽어 넘기면
  `auto-record.js`가 확실한 것만 기록으로 넣고 나머지는 알림함에 남김. 고정지출과 짝이
  맞으면 처리 완료로 표시해 중복을 막음
- **공유로 기록 화면 채우기**(2026-09-08) — `share_target`으로 문자를 넘기면 금액·가맹점·
  날짜가 채워진 채로 기록 화면이 열림
- **자동 백업**(2026-09-08) — 껍데기가 화면을 벗어날 때 `Download/내돈챙겨줘/`에 하루 한
  파일, 30일치

### 2026-08-22: 앱 전체 칩버튼 → 셀렉트 정리
카테고리/카드/통장처럼 "여럿 중 하나 고르기"는 전부 `<select>`로 통일(기록·수정·
할부·홈의 대중교통/입출금/이체 등). 예외적으로 남겨둔 것:
- **결제수단(카드/현금/할부) 같은 화면 전환용 3-way 버튼**은 그대로 — 값이 아니라
  모드를 바꾸는 것이라 셀렉트로 옮기지 않음
- **빠른금액 버튼(`QuickAmountButtons`)**은 기록·통장/카드 신규등록처럼 "빠르게 새로
  입력"하는 곳엔 남기고, 수정·조정처럼 "이미 있는 값 고치는" 곳에서만 뺌
- 설정 화면의 "카드값 수동 추가/초기화"는 별도 섹션을 없애고 "카드 관리" 목록 각
  줄의 "조정" 버튼으로 흡수(카드 정보가 두 군데 흩어져 있던 걸 한 곳으로)

---

## 8. 남은 작업 / 알려진 제약

### PENDING
- **기록 속도 개선** — "자주 쓰는 카테고리/카드 기본값"은 2026-08-22에 완료(`findRecentDefaults`,
  최근 지출 기준 자동). "즐겨찾기 원터치 재등록"은 아직 미착수
- ~~할부 종료 알림~~ — 2026-08-22 완료. `fixedInfo()`가 `isLast` 반환, 표시되는 3곳(홈/기록/내역)에 "마지막" 배지
- **다른 카드사/은행 문자 파싱** — 현대카드만 검증. 실제 케이스 나오면 그때 다듬기로 함
- **카드값 `결제하기`를 누르면 여유가 늘어남** (2026-09-08 확인, 사용자 합의 필요)
  — `payCard`가 `bill`을 0으로 만들면서 `cardBillTotal`이 그만큼 줄어 여유가 늘어남.
  돈은 방금 나갔는데 안 쓴 것이 됨. 고치려면 카드를 '이번 달 카드지출'로 세야 하는데,
  그러면 지난달 미결제분이 이번 달 여유에서 빠짐. **어느 쪽이 맞는지는 쓰는 사람이
  정할 문제**라 손대지 않고 남겨 둠
- ~~껍데기가 남긴 백업을 앱에서 못 읽음~~ — 2026-09-09 완료. 설정 → 데이터 백업 →
  '휴대폰에 저장된 백업'에서 날짜를 골라 되돌릴 수 있음

### 구조적 제약
- **푸시 알림 불가** — 웹앱이라 "오늘 기록 안 하셨어요" 같은 리마인드 못 보냄
- ~~**알림 자동 캐치 불가**~~ — 2026-09-08에 안드로이드 껍데기(`NotificationListener`)로
  해결. 서버·DB는 필요 없었음(껍데기가 WebView 안의 웹앱에 직접 넘김). **iOS는 여전히
  원천 불가**이고, 크롬으로 열면 예전처럼 붙여넣기·공유만 됨
- **데이터가 기기 안에만 존재** — 브라우저 데이터 삭제하면 복구 불가. 백업 안내 필요

### 과거 버그 (수정 완료, 재발 주의)
- 정산된 대리결제 삭제 시 카드값이 안 빠지던 버그 → `settlementCardDelta` 등을 지출에 저장해 되돌림
- 카드 맞추기 이중계산 → `reconcileCard`에서 `fixedPortion` 차감
- 결제하기가 일시불만 출금하던 버그 → `bill + fixedPortion` 전액 출금으로 수정
- 문자 파싱이 "누적" 금액·마스킹 이름을 메모에 넣던 버그
- 정산받은 금액(`reimbursedAmount`)을 어느 합계도 안 읽던 버그 → `netAmount()`로 통일
- 자동 등록이 카드 할부와 짝을 지어 그 달 카드값이 두 배가 되던 버그 (2026-09-09)
- 문자 날짜가 미래로 밀리거나 `2026-19-99` 같은 값이 되어 기록이 어느 달에도 안 잡히던
  버그 (2026-09-09)

**"라벨이 말하는 것과 실제 계산이 다르다"는 이 저장소에서 다섯 번 나왔다**(최신순=등록순,
총 지출 건수 불일치, 지난달 대비의 기간 길이, 정산받은 금액 누락, 자동 등록이 할부와 짝을
지어 카드값 두 배). 새 요약·비교 숫자를 만들 때는 **라벨이 말하는 것과 실제 계산 대상을
반드시 대조할 것.**

### 2026-08-22~23: 전체 코드 훑기 — "삭제된 카드/통장을 계속 참조" 버그 계열
사용자 요청("1부터 100까지 전부다 확인")으로 전체를 다시 훑으면서, 카드/통장을 삭제한
뒤에도 다른 데이터가 그 id를 계속 들고 있어서 생기는 같은 패턴의 버그를 여러 곳에서
발견·수정함. 공통 패턴: `f.cardId || data.cards[0]?.id` 처럼 **falsy만 걸러내는 폴백은
"삭제된 카드의 id"(truthy)를 못 잡아낸다** — 그러면 실제로는 아무 카드/통장도 안
바뀌었는데 성공 토스트만 뜨는 조용한 실패가 생김. 고친 곳:
- `fixedSum`이 "카드반영" 누른 뒤에도 그 항목을 이중으로 잡던 버그 (App.jsx)
- `autoProcessFixed`의 `autoPayDay=31`이 30일 이하 달엔 영원히 안 걸리던 버그, `aid` 유효성 (lib/data.js)
- `TransitQuickAdd`/`markFixedPaid`/Ledger `startEdit`/Add `findRecentDefaults` — 삭제된
  카드·통장을 기본값으로 들고 있던 것들에 `data.cards.some(...)` 검증 추가
- `removeBalance`가 연결된 `fixedExpenses[].paidMonths` 마커를 안 지우던 것
- `isCardAdjustment` 지출을 수정할 때 결제수단/카드를 바꿀 수 있어서 원본 고정지출
  마커와 어긋날 수 있던 것 → 수정 폼에서 그 필드들 비활성화
- **`settleReceivable`의 `settlementCardId`**(Home.jsx) — 대리결제 원래 카드가 삭제된
  상태로 부족분 정산하면 어느 카드값에도 안 반영되면서 "카드값에 반영됐어요" 토스트만
  뜨던 것
- **`removeCard`가 `fixedExpenses`를 안 건드리던 것**(Settings.jsx) — 할부/카드매달반복이
  삭제된 카드를 계속 가리키면 `cardTotals.fixedPortion` 집계에서 안 잡혀서 그 금액이
  예산 계산(`spent`)에서 통째로 조용히 빠짐. 카드 삭제 시 남은 첫 카드로 재배정하도록
  수정 + 확인창에 영향받는 건수 안내 추가. `App.jsx`의 `fixedPortion` 매칭도 방어적으로
  보강(옛 백업으로 들어온 유효하지 않은 cardId 대비)
- 안 쓰이던 `StatCard` 컴포넌트 제거 (`components/common.jsx`)

검토했지만 **문제없다고 확인**한 것: `unmarkFixedPaid`/`remove()`/`saveEdit()`의 카드값
"되돌리기" 폴백은 삭제된 카드를 가리켜도 안전함 — 되돌릴 대상 자체가 카드와 함께
이미 사라졌으니 아무것도 안 하는 게 맞는 동작.

---

## 9. 용어 통일 (UI 문구)

| 쓰는 말 | 안 쓰는 말 |
|---|---|
| 대리결제(추후 정산) | 채권 |
| 할부(고정지출) | 할부(대출) |
| 통장(자동이체) | 통장(대출/자동이체) |
| 반영 전 여유 | 실제 여유 |
| 표기내역 (모든 placeholder) | 예: 월세, 쏘렌토할부 등 |

- 목표지출액 기준이며 **급여일 개념 없음** (달력상 1일~말일)
- 카드 결제하기(bill 전액 출금)는 **홈에서만**. 대리결제 정산은 2026-08-21부터 **홈·내역 둘 다** 가능

---

## 10. 안드로이드 껍데기 (2026-09-08)

**위치: `D:\passbook-android` — 이 저장소 밖의 별도 폴더.** 패키지 `kr.gct.passbook`,
minSdk 29 / targetSdk 34.

웹앱은 알림을 읽을 수 없어서(권한 문제가 아니라 **기능이 아예 없음**) 그것만 네이티브로
뺐음. **화면은 하나도 새로 만들지 않음** — WebView로 배포된 이 앱을 그대로 띄움
(`res/values/strings.xml`의 `app_url`이 Vercel 주소를 가리킴). 그래서 **웹만 고쳤으면
APK를 다시 깔 필요가 없음** — 푸시하면 앱을 다시 켤 때 반영됨. 네이티브(알림 규칙·백업·
앱 이름)를 고칠 때만 재설치.

### 구성
```
MainActivity.kt    WebView + window.PassbookNative 브리지
PaymentListener.kt NotificationListenerService — 결제로 보이는 알림만 골라 큐에 담음
PaymentQueue.kt    담아 둔 알림. drain()하면 비워짐
BackupStore.kt     Download/내돈챙겨줘/ 에 하루 한 파일, 30일치(KEEP=30)
```

브리지가 웹에 내주는 것(웹 쪽 짝은 `src/lib/native.js`):
`pullPending` · `pendingCount` · `hasNotificationAccess` ·
`openNotificationAccessSettings` · `saveBackup` · `listBackups` · `readBackup`.

### 어떤 알림을 담나 (`PaymentListener.kt`)
- **금융 앱**(패키지 이름에 `bank`/`card`/`pay`/카드사·은행 이름)이면 **금액만 있어도** 담음
- **그 밖(문자 앱 등)**은 금액과 낱말(`승인`·`결제`·`출금`·`입금`·`이체`·`사용`·`취소`·
  `매출`·`청구`)이 **둘 다** 있어야 담음. 카드 결제 문자가 대개 삼성·구글 메시지로 오기
  때문에 그쪽도 목록에 넣어 둠
- 금액은 `[\d,]{3,}\s*원` — 숫자·쉼표가 **세 자 이상**이어야 함(100원부터 걸림).
  "3원" 같은 잡음을 거르는 용도. (`PaymentListener.kt`의 주석은 "1,000원 이상"이라고
  적혀 있는데 실제 정규식은 세 자리부터 통과함 — 주석 쪽이 틀림)

여기서 **담는 것과 자동으로 기록에 넣는 것은 다름.** 껍데기는 넓게 담고, 좁게 거르는
일은 웹의 `auto-record.js`가 함(§4). 담긴 것 중 자동으로 못 넣은 건 기록 탭 알림함에 남음.

### 빌드
```
D:\dev\jdk17          JDK
D:\devndroid-sdk    SDK
D:\dev\gradle-8.7     Gradle
```
```
JAVA_HOME=D:\dev\jdk17  gradle assembleRelease --no-daemon
결과: appuild\outputspk
eleasepp-release.apk
```

### ⚠️ 서명 키
**키를 잃으면 업데이트를 덮어 설치할 수 없음.** 지웠다 깔아야 하고, 그러면 앱 안의
localStorage가 비면서 **기록이 전부 날아감.**

```
키: E:\클로드백업\passbook.jks   (비밀번호 passbook, alias passbook)
```

### 자동 백업
껍데기가 화면을 벗어날 때 `Download/내돈챙겨줘/`에 하루 한 파일씩, 30일치를 남김.
앱 전용 폴더가 아닌 이유는 **앱을 지워도 파일이 남아야 하기 때문.** 다만 지금은 웹에서
이 백업을 읽는 화면이 없어서(§8 PENDING), 복원하려면 파일을 직접 찾아 설정 → 가져오기에
붙여넣어야 함.
